  package      org . uma . jmetal . util . grouping . impl ;   import   java . util . ArrayList ;  import   java . util . List ;  import      org . uma . jmetal . util . errorchecking . Check ;  import      org . uma . jmetal . util . grouping . CollectionGrouping ;   public abstract class ListGrouping  <  C  extends  Comparable  < C > >  implements   CollectionGrouping  <  List  < C > >  {   protected final  int  numberOfGroups ;   protected  List  < Integer >  indices ;   protected  List  <  List  < Integer > >  groups ;   public ListGrouping  (   int numberOfGroups )  {    this . numberOfGroups = numberOfGroups ;    this . groups =  new  ArrayList  < >  ( numberOfGroups ) ; }   public abstract void computeGroups  (   List  < C > list ) ;   protected void createGroups  ( )  {   int  groupSize =  computeGroupSize  ( ) ;   int  index =  groupValues  ( groupSize ) ;   fillRemainingIndices  ( index ) ; }   private  int groupValues  (   int groupSize )  {   int  index = 0 ;  for (   int  i = 0 ;  i < numberOfGroups ;  i ++ )  {   groups . add  (  new  ArrayList  < >  ( ) ) ;  for (   int  j = 0 ;  j < groupSize ;  j ++ )  {    groups . get  ( i ) . add  (  indices . get  (  index ++ ) ) ; } }  return index ; }   private void fillRemainingIndices  (   int index )  {   int  lastGroupIndex =   groups . size  ( ) - 1 ;  while  (  index <  indices . size  ( ) )  {    groups . get  ( lastGroupIndex ) . add  (  indices . get  (  index ++ ) ) ; } }   private  int computeGroupSize  ( )  {  return   indices . size  ( ) / numberOfGroups ; }    @ Override public  int numberOfGroups  ( )  {  return numberOfGroups ; }    @ Override public  List  < Integer > getGroup  (   int groupIndex )  {   Check . that  (  (   (  groupIndex >= 0 ) &&  (  groupIndex < numberOfGroups ) ) ,   "The group index " + groupIndex + " is invalid" ) ;  return  groups . get  ( groupIndex ) ; } }