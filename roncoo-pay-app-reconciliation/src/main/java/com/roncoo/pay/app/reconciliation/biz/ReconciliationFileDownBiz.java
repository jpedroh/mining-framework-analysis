  package      com . roncoo . pay . app . reconciliation . biz ;   import   java . io . File ;  import   java . util . Date ;  import       com . roncoo . pay . common . core . utils . StringUtil ;  import       com . roncoo . pay . reconciliation . fileDown . service . ReconciliationFactory ;  import     org . apache . commons . logging . Log ;  import     org . apache . commons . logging . LogFactory ;  import      org . springframework . beans . factory . annotation . Autowired ;  import    org . springframework . stereotype . Component ;    @ Component  ( "reconciliationFileDownBiz" ) public class ReconciliationFileDownBiz  {   private static final Log  LOG =  LogFactory . getLog  (  ReconciliationFileDownBiz . class ) ;   private static final  int  DOWNLOAD_TRY_TIMES = 3 ;    @ Autowired private ReconciliationFactory  reconciliationFactory ;   public File downReconciliationFile  (  String interfaceCode ,  Date billDate )  {  if  (  StringUtil . isEmpty  ( interfaceCode ) )  {   LOG . info  ( "支付渠道编码为空" ) ;  return null ; }  return  this . downFile  ( interfaceCode , billDate ) ; }   private File downFile  (  String interfaceCode ,  Date billDate )  {   LOG . info  (   "银行渠道编号[" + interfaceCode + "],进入下载业务对账文件操作>>>" ) ;  try  {  File  file = null ;   int  downloadTrytimes = 0 ;  while  (   file == null &&  downloadTrytimes < DOWNLOAD_TRY_TIMES )  {  try  {   downloadTrytimes ++ ;   file =  reconciliationFactory . fileDown  ( interfaceCode , billDate ) ; }  catch (   Exception e )  {   LOG . error  ( "下载账单文件失败" , e ) ;   Thread . sleep  ( 10000 ) ; } }  return file ; }  catch (   Exception e )  {   LOG . error  ( "下载微信账单文件失败" , e ) ; }  return null ; } }