  package      com . roncoo . pay . reconciliation . fileDown . impl ;   import   java . io . File ;  import   java . io . IOException ;  import   java . text . SimpleDateFormat ;  import   java . util . Date ;  import   java . util . HashMap ;  import   java . util . Iterator ;  import    java . util . Map . Entry ;  import       com . roncoo . pay . reconciliation . fileDown . service . FileDown ;  import     org . apache . commons . logging . Log ;  import     org . apache . commons . logging . LogFactory ;  import     com . alibaba . druid . util . StringUtils ;  import      com . roncoo . pay . reconciliation . utils . FileUtils ;  import      com . roncoo . pay . reconciliation . utils . SignHelper ;  import      com . roncoo . pay . reconciliation . utils . WeiXinBaseUtils ;  import       com . roncoo . pay . reconciliation . utils . https . HttpClientUtil ;  import       com . roncoo . pay . reconciliation . utils . https . HttpResponse ;  import      com . roncoo . pay . trade . utils . WeixinConfigUtil ;   public class WinXinFileDown  implements  FileDown  {   private static final Log  LOG =  LogFactory . getLog  (  WinXinFileDown . class ) ;   private String  url =  WeixinConfigUtil . readConfig  ( "download_bill_url" ) ;   private String  appid =  WeixinConfigUtil . readConfig  ( "appId" ) ; ;   private String  mch_id =  WeixinConfigUtil . readConfig  ( "mch_id" ) ;   private String  bill_date ;   private String  appSecret =  WeixinConfigUtil . readConfig  ( "partnerKey" ) ;   private String  bill_type =  WeixinConfigUtil . readConfig  ( "bill_type" ) ;   public File fileDown  (  Date billDate ,  String dir )  throws IOException  {  SimpleDateFormat  sdf =  new SimpleDateFormat  ( "yyyyMMdd" ) ;   bill_date =  sdf . format  ( billDate ) ;  HttpResponse  response = null ;  try  {  String  xml =  this . generateXml  ( ) ;   LOG . info  ( xml ) ;   response =  HttpClientUtil . httpsRequest  ( url , "POST" , xml ) ;  File  file =  new File  ( dir ,    bill_date + "_" +  bill_type . toLowerCase  ( ) + ".txt" ) ;   int  index = 1 ;  while  (  file . exists  ( ) )  {   file =  new File  ( dir ,     bill_date + "_" +  bill_type . toLowerCase  ( ) + index + ".txt" ) ;   index ++ ; }  return  FileUtils . saveFile  ( response , file ) ; }  catch (   IOException e )  {  throw  new IOException  ( "下载微信账单失败" , e ) ; }  finally  {  try  {  if  (  response != null )  {   response . close  ( ) ; } }  catch (   IOException e )  {   LOG . error  ( "关闭下载账单的流/连接失败" , e ) ; } } }   public String generateXml  ( )  {   HashMap  < String , String >  params =  new  HashMap  < String , String >  ( ) ;   params . put  ( "appid" , appid ) ;   params . put  ( "mch_id" , mch_id ) ;   params . put  ( "bill_date" , bill_date ) ;   params . put  ( "bill_type" , bill_type ) ;   params . put  ( "nonce_str" ,  WeiXinBaseUtils . createNoncestr  ( ) ) ;  for (   Iterator  <  Entry  < String , String > >  it =   params . entrySet  ( ) . iterator  ( ) ;  it . hasNext  ( ) ; )  {   Entry  < String , String >  entry =  it . next  ( ) ;  if  (  StringUtils . isEmpty  (  entry . getValue  ( ) ) )  {   it . remove  ( ) ; } }  String  sign =  SignHelper . getSign  ( params , appSecret ) ;   params . put  ( "sign" ,  sign . toUpperCase  ( ) ) ;  return  WeiXinBaseUtils . arrayToXml  ( params ) ; } }