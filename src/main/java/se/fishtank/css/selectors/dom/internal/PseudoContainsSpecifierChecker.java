  package      se . fishtank . css . selectors . dom . internal ;   import   java . util . LinkedHashSet ;  import   java . util . Set ;  import    org . w3c . dom . Node ;  import     se . fishtank . css . selectors . NodeSelectorException ;  import      se . fishtank . css . selectors . specifier . PseudoContainsSpecifier ;  import     se . fishtank . css . util . Assert ;   public class PseudoContainsSpecifierChecker  extends NodeTraversalChecker  {   private final PseudoContainsSpecifier  specifier ;   public PseudoContainsSpecifierChecker  (  PseudoContainsSpecifier specifier )  {   Assert . notNull  ( specifier , "specifier is null!" ) ;    this . specifier = specifier ; }    @ Override public  Set  < Node > check  (   Set  < Node > nodes ,  Node root )  throws NodeSelectorException  {   Assert . notNull  ( nodes , "nodes is null!" ) ;   Assert . notNull  ( root , "root is null!" ) ;   LinkedHashSet  < Node >  result =  new  LinkedHashSet  < Node >  ( ) ;  String  value =  specifier . getValue  ( ) ;  for ( Node node : nodes )  {  String  textContent =  node . getTextContent  ( ) ;  if  (   textContent != null &&  textContent . contains  ( value ) )  {   result . add  ( node ) ; } }  return result ; } }