  package    net . logstash . logback . appender ;   import   java . io . BufferedOutputStream ;  import   java . io . IOException ;  import   java . io . OutputStream ;  import   java . net . InetAddress ;  import   java . net . InetSocketAddress ;  import   java . net . Socket ;  import   java . net . UnknownHostException ;  import    java . nio . charset . Charset ;  import    java . util . concurrent . BlockingQueue ;  import    java . util . concurrent . CountDownLatch ;  import    java . util . concurrent . RejectedExecutionException ;  import    java . util . concurrent . ScheduledFuture ;  import    java . util . concurrent . TimeUnit ;  import   javax . net . SocketFactory ;  import    javax . net . ssl . SSLContext ;  import    javax . net . ssl . SSLSocket ;  import    javax . net . ssl . SSLSocketFactory ;  import     net . logstash . logback . encoder . SeparatorParser ;  import      ch . qos . logback . core . encoder . Encoder ;  import       ch . qos . logback . core . net . ssl . ConfigurableSSLSocketFactory ;  import       ch . qos . logback . core . net . ssl . SSLConfigurableSocket ;  import       ch . qos . logback . core . net . ssl . SSLConfiguration ;  import       ch . qos . logback . core . net . ssl . SSLParametersConfiguration ;  import      ch . qos . logback . core . spi . DeferredProcessingAware ;  import      ch . qos . logback . core . status . ErrorStatus ;  import      ch . qos . logback . core . util . CloseUtil ;  import      ch . qos . logback . core . util . Duration ;  import    com . lmax . disruptor . EventHandler ;  import    com . lmax . disruptor . LifecycleAware ;  import    com . lmax . disruptor . RingBuffer ;  import   java . util . ArrayList ;  import   java . util . Collections ;  import   java . util . List ;  import     org . apache . commons . lang . StringUtils ;   public abstract class AbstractLogstashTcpSocketAppender  <  Event  extends DeferredProcessingAware >  extends  AsyncDisruptorAppender  < Event >  {   public static final  int  DEFAULT_PORT = 4560 ;   public static final  int  DEFAULT_RECONNECTION_DELAY = 30000 ;   public static final  int  DEFAULT_QUEUE_SIZE = DEFAULT_RING_BUFFER_SIZE ;   public static final  int  DEFAULT_CONNECTION_TIMEOUT = 5000 ;   public static final  int  DEFAULT_WRITE_BUFFER_SIZE = 8192 ;   private String  remoteHost ;   private  int  port = DEFAULT_PORT ;   private Duration  reconnectionDelay =  new Duration  ( DEFAULT_RECONNECTION_DELAY ) ;   private  int  acceptConnectionTimeout = DEFAULT_CONNECTION_TIMEOUT ;   private String  peerId ;   private  Encoder  < Event >  encoder ;   private  int  writeBufferSize = DEFAULT_WRITE_BUFFER_SIZE ;   private SocketFactory  socketFactory ;   private SSLConfiguration  sslConfiguration ;   private Duration  keepAliveDuration ;   private String  keepAliveMessage =  System . getProperty  ( "line.separator" ) ;   private Charset  keepAliveCharset =  Charset . forName  ( "UTF-8" ) ;   private   byte  [ ]  keepAliveBytes ;   private volatile CountDownLatch  shutdownLatch ;   private class TcpSendingEventHandler  implements   EventHandler  <  LogEvent  < Event > > , LifecycleAware  {   private static final  int  MAX_REPEAT_CONNECTION_ERROR_LOG = 5 ;   private static final  int  MAX_REPEAT_WRITE_ATTEMPTS = 5 ;   private volatile Socket  socket ;   private volatile OutputStream  outputStream ;   private volatile  long  lastSentTimestamp ;   private  ScheduledFuture  <  ? >  keepAliveFuture ;   private KeepAliveRunnable  keepAliveRunnable ;   private class KeepAliveRunnable  implements  Runnable  {    @ Override public void run  ( )  {   long  lastSent = lastSentTimestamp ;   long  currentTime =  System . currentTimeMillis  ( ) ;  if  (  hasKeepAliveDurationElapsed  ( lastSent , currentTime ) )  {     getDisruptor  ( ) . getRingBuffer  ( ) . publishEvent  (  getEventTranslator  ( ) , null ) ;   scheduleKeepAlive  ( currentTime ) ; } else  {   scheduleKeepAlive  ( lastSent ) ; } } }    @ Override public void onEvent  (   LogEvent  < Event > logEvent ,   long sequence ,  boolean endOfBatch )  throws Exception  {  for (   int  i = 0 ;  i < MAX_REPEAT_WRITE_ATTEMPTS ;  i ++ )  {  if  (   this . socket == null )  {  return ; }  try  {   long  currentTime =  System . currentTimeMillis  ( ) ;  if  (   logEvent . event != null )  {   encoder . doEncode  (  logEvent . event ) ; } else  if  (  hasKeepAliveDurationElapsed  ( lastSentTimestamp , currentTime ) )  {   outputStream . write  ( keepAliveBytes ) ; }  if  ( endOfBatch )  {   outputStream . flush  ( ) ; }   lastSentTimestamp = currentTime ;  if  (  shouldCloseConnection  ( currentTime ) )  {   addInfo  (  peerId + "closing connection and attempt to reconnect to primary server." ) ;   outputStream . flush  ( ) ;   reopenSocket  ( ) ; }  break ; }  catch (   Exception e )  {   addWarn  (   peerId + "unable to send event: " +  e . getMessage  ( ) , e ) ;   reopenSocket  ( ) ; } } }   private boolean hasKeepAliveDurationElapsed  (   long lastSent ,   long currentTime )  {  return   isKeepAliveEnabled  ( ) &&   lastSent +  keepAliveDuration . getMilliseconds  ( ) < currentTime ; }    @ Override public void onStart  ( )  {   openSocket  ( ) ;   scheduleKeepAlive  (  System . currentTimeMillis  ( ) ) ; }    @ Override public void onShutdown  ( )  {   unscheduleKeepAlive  ( ) ;   closeEncoder  ( ) ;   closeSocket  ( ) ; }   private synchronized void reopenSocket  ( )  {   closeSocket  ( ) ;   openSocket  ( ) ; }   private synchronized void openSocket  ( )  {   int  hostIndex = 0 ;   int  errorCount = 0 ;  while  (   isStarted  ( ) &&  !   Thread . currentThread  ( ) . isInterrupted  ( ) )  {   long  startTime =  System . currentTimeMillis  ( ) ;  Socket  tempSocket = null ;  OutputStream  tempOutputStream = null ;  try  {  HostInfo  currentHost =   getDestinations  ( ) . get  ( hostIndex ) ;   peerId =   "Log destination " + currentHost + ": " ;   tempSocket =  socketFactory . createSocket  ( ) ;   tempSocket . setSoTimeout  ( acceptConnectionTimeout ) ;   tempSocket . connect  (  new InetSocketAddress  (  currentHost . host ,  currentHost . port ) , acceptConnectionTimeout ) ;   tempOutputStream =  new BufferedOutputStream  (  tempSocket . getOutputStream  ( ) , writeBufferSize ) ;   encoder . init  ( tempOutputStream ) ;   addInfo  (  peerId + "connection established." ) ;    this . socket = tempSocket ;    this . outputStream = tempOutputStream ;  if  (   reattemptPrimaryConnectionDelay != null &&  hostIndex != 0 )  {   secondaryConnectionTTL =  startTime +  reattemptPrimaryConnectionDelay . getMilliseconds  ( ) ; } else  {   secondaryConnectionTTL =  Long . MAX_VALUE ; }  return ; }  catch (   Exception e )  {   CloseUtil . closeQuietly  ( tempOutputStream ) ;   CloseUtil . closeQuietly  ( tempSocket ) ;   hostIndex ++ ;  if  (  hostIndex >=  destinations . size  ( ) )  {   hostIndex = 0 ;   long  sleepTime =  Math . max  ( 0 ,   reconnectionDelay . getMilliseconds  ( ) -  (   System . currentTimeMillis  ( ) - startTime ) ) ;  if  (   errorCount ++ <  MAX_REPEAT_CONNECTION_ERROR_LOG *  destinations . size  ( ) )  {   addWarn  (    peerId + "connection failed. Waiting " + sleepTime + "ms before attempting reconnection." , e ) ; }  if  (  sleepTime > 0 )  {  try  {   shutdownLatch . await  ( sleepTime ,  TimeUnit . MILLISECONDS ) ; }  catch (   InterruptedException ie )  {    Thread . currentThread  ( ) . interrupt  ( ) ;   addWarn  (  peerId + "connection interrupted. Will no longer attempt reconnection." ) ; } } } else  {  if  (   errorCount ++ <  MAX_REPEAT_CONNECTION_ERROR_LOG *  destinations . size  ( ) )  {   addWarn  (    peerId + "connection failed. Retry with next server (" +   getDestinations  ( ) . get  ( hostIndex ) + ")." , e ) ; } } } } }   private synchronized void closeSocket  ( )  {   CloseUtil . closeQuietly  ( outputStream ) ;   outputStream = null ;   CloseUtil . closeQuietly  ( socket ) ;   socket = null ; }   private void closeEncoder  ( )  {  try  {   encoder . close  ( ) ; }  catch (   IOException ioe )  {   addStatus  (  new ErrorStatus  (   "Failed to close encoder for appender named [" + name + "]." , this , ioe ) ) ; }   encoder . stop  ( ) ; }   private synchronized void scheduleKeepAlive  (   long basedOnTime )  {  if  (   isKeepAliveEnabled  ( ) &&  !   Thread . currentThread  ( ) . isInterrupted  ( ) )  {  if  (  keepAliveRunnable == null )  {   keepAliveRunnable =  new KeepAliveRunnable  ( ) ; }   long  delay =   keepAliveDuration . getMilliseconds  ( ) -  (   System . currentTimeMillis  ( ) - basedOnTime ) ;  try  {   keepAliveFuture =   getExecutorService  ( ) . schedule  ( keepAliveRunnable , delay ,  TimeUnit . MILLISECONDS ) ; }  catch (   RejectedExecutionException e )  {   keepAliveFuture = null ; } } }   private synchronized void unscheduleKeepAlive  ( )  {  if  (  keepAliveFuture != null )  {   keepAliveFuture . cancel  ( true ) ;  try  {   keepAliveFuture . get  ( ) ; }  catch (   InterruptedException e )  {    Thread . currentThread  ( ) . interrupt  ( ) ; }  catch (   Exception e )  { } } }   private volatile  long  secondaryConnectionTTL =  Long . MAX_VALUE ;   private boolean shouldCloseConnection  (   long currentTime )  {  return  secondaryConnectionTTL <= currentTime ; } }   private static class UnconnectedConfigurableSSLSocketFactory  extends ConfigurableSSLSocketFactory  {   private final SSLParametersConfiguration  parameters ;   private final SSLSocketFactory  delegate ;   public UnconnectedConfigurableSSLSocketFactory  (  SSLParametersConfiguration parameters ,  SSLSocketFactory delegate )  {  super  ( parameters , delegate ) ;    this . parameters = parameters ;    this . delegate = delegate ; }    @ Override public Socket createSocket  ( )  throws IOException  {  SSLSocket  socket =  ( SSLSocket )  delegate . createSocket  ( ) ;   parameters . configure  (  new SSLConfigurableSocket  ( socket ) ) ;  return socket ; } }   public AbstractLogstashTcpSocketAppender  ( )  {  super  ( ) ;   setEventHandler  (  new TcpSendingEventHandler  ( ) ) ; }    @ Override public boolean isStarted  ( )  {  CountDownLatch  latch =  this . shutdownLatch ;  return   latch != null &&   latch . getCount  ( ) != 0 ; }   public synchronized void start  ( )  {  if  (  isStarted  ( ) )  {  return ; }   int  errorCount = 0 ;  if  (  encoder == null )  {   errorCount ++ ;   addError  (   "No encoder was configured for appender " + name + "." ) ; }  if  (    destinations . size  ( ) > 0 &&  remoteHost != null )  {   errorCount ++ ;   addError  ( "Use '<remoteHost>/<port>' or '<destination>' but not both" ) ; }  if  (  remoteHost != null )  {   addWarn  ( "<remoteHost>/<port> are DEPRECATED, please use <destination> instead" ) ;  try  {   addDestination  ( remoteHost , port ) ; }  catch (   IllegalArgumentException e )  {   errorCount ++ ;   addError  (  e . getMessage  ( ) ) ; } }  if  (   destinations . size  ( ) == 0 )  {   errorCount ++ ;   addError  ( "No destination was configured. Please use <destination> to add one or more destinations to the appender" ) ; }  if  (   errorCount == 0 &&  socketFactory == null )  {  if  (  sslConfiguration == null )  {   socketFactory =  SocketFactory . getDefault  ( ) ; } else  {  try  {  SSLContext  sslContext =   getSsl  ( ) . createContext  ( this ) ;  SSLParametersConfiguration  parameters =   getSsl  ( ) . getParameters  ( ) ;   parameters . setContext  (  getContext  ( ) ) ;   socketFactory =  new UnconnectedConfigurableSSLSocketFactory  ( parameters ,  sslContext . getSocketFactory  ( ) ) ; }  catch (   Exception e )  {   addError  ( "Unable to create ssl context" , e ) ;   errorCount ++ ; } } }  if  (   keepAliveMessage != null &&  keepAliveCharset != null )  {   keepAliveBytes =  keepAliveMessage . getBytes  ( keepAliveCharset ) ; }  if  (  errorCount == 0 )  {  if  (   getThreadNamePrefix  ( ) == DEFAULT_THREAD_NAME_PREFIX )  {   setThreadNamePrefix  (     DEFAULT_THREAD_NAME_PREFIX + remoteHost + ":" + port + "-" ) ; }   encoder . setContext  (  getContext  ( ) ) ;  if  (  !  encoder . isStarted  ( ) )  {   encoder . start  ( ) ; }  if  (  keepAliveDuration != null )  {   setThreadPoolCoreSize  (   getThreadPoolCoreSize  ( ) + 1 ) ; }    this . shutdownLatch =  new CountDownLatch  ( 1 ) ;   super . start  ( ) ; } }    @ Override public synchronized void stop  ( )  {  if  (  !  isStarted  ( ) )  {  return ; }    this . shutdownLatch . countDown  ( ) ;   super . stop  ( ) ; }   public  Encoder  < Event > getEncoder  ( )  {  return encoder ; }   public void setEncoder  (   Encoder  < Event > encoder )  {    this . encoder = encoder ; }   public SocketFactory getSocketFactory  ( )  {  return socketFactory ; }   public void setSocketFactory  (  SocketFactory socketFactory )  {    this . socketFactory = socketFactory ; }   public  @ Deprecated void setRemoteHost  (  String host )  {   remoteHost = host ; }   public  @ Deprecated String getRemoteHost  ( )  {  return remoteHost ; }   public  @ Deprecated void setPort  (   int port )  {    this . port = port ; }   public  @ Deprecated  int getPort  ( )  {  return port ; }   public void setReconnectionDelay  (  Duration delay )  {  if  (   delay == null ||   delay . getMilliseconds  ( ) <= 0 )  {  throw  new IllegalArgumentException  ( "reconnectionDelay must be > 0" ) ; }    this . reconnectionDelay = delay ; }   public Duration getReconnectionDelay  ( )  {  return reconnectionDelay ; }  void setAcceptConnectionTimeout  (   int acceptConnectionTimeout )  {    this . acceptConnectionTimeout = acceptConnectionTimeout ; }   public  int getWriteBufferSize  ( )  {  return writeBufferSize ; }   public void setWriteBufferSize  (   int writeBufferSize )  {    this . writeBufferSize = writeBufferSize ; }   public  int getQueueSize  ( )  {  return  getRingBufferSize  ( ) ; }   public void setQueueSize  (   int queueSize )  {   setRingBufferSize  ( queueSize ) ; }   public SSLConfiguration getSsl  ( )  {  return sslConfiguration ; }   public void setSsl  (  SSLConfiguration sslConfiguration )  {    this . sslConfiguration = sslConfiguration ; }   public Duration getKeepAliveDuration  ( )  {  return keepAliveDuration ; }   public void setKeepAliveDuration  (  Duration keepAliveDuration )  {    this . keepAliveDuration = keepAliveDuration ; }   public String getKeepAliveMessage  ( )  {  return keepAliveMessage ; }   public void setKeepAliveMessage  (  String keepAliveMessage )  {    this . keepAliveMessage =  SeparatorParser . parseSeparator  ( keepAliveMessage ) ; }   public boolean isKeepAliveEnabled  ( )  {  return    this . keepAliveDuration != null &&   this . keepAliveMessage != null ; }   public Charset getKeepAliveCharset  ( )  {  return keepAliveCharset ; }   public void setKeepAliveCharset  (  Charset keepAliveCharset )  {    this . keepAliveCharset = keepAliveCharset ; }   private  List  < HostInfo >  destinations =  new  ArrayList  < HostInfo >  ( 2 ) ;   private Duration  reattemptPrimaryConnectionDelay = null ;   private static class HostInfo  {   private final String  host ;   private final  int  port ;   public HostInfo  (  String host ,   int port )  {    this . host = host ;    this . port = port ; }   public String toString  ( )  {  return   host + ":" + port ; } }   public void addDestination  (   final String destination )  throws IllegalArgumentException  {   String  [ ]  destinations =  StringUtils . split  (  StringUtils . trimToEmpty  ( destination ) , ',' ) ;  for ( String entry : destinations )  {   String  [ ]  parts =  StringUtils . split  ( entry , ':' ) ;  if  (   parts . length > 2 )  {  throw  new IllegalArgumentException  (   "Invalid destination '" + entry + "': unparseable value (expected format 'host[:port]')." ) ; }  String  host =  StringUtils . trim  (  parts [ 0 ] ) ;   int  port = DEFAULT_PORT ;  if  (   parts . length == 2 )  {  try  {   port =  Integer . parseInt  (  StringUtils . trim  (  parts [ 1 ] ) ) ; }  catch (   NumberFormatException e )  {  throw  new IllegalArgumentException  (     "Invalid destination '" + entry + "': unparseable port (was '" +  parts [ 1 ] + "')." ) ; } }   addDestination  ( host , port ) ; } }   public void addDestination  (   final String host ,   final  int port )  throws IllegalArgumentException  {  try  {   InetAddress . getByName  ( host ) ; }  catch (   UnknownHostException ex )  {  throw  new IllegalArgumentException  (     "Invalid destination '" + host + "': host unknown (was '" + host + "')." ) ; }  if  (  port <= 0 )  {  throw  new IllegalArgumentException  (       "Invalid destination '" + host + ":" + port + "': port must be greater than 0 (was " + port + ")." ) ; }   destinations . add  (  new HostInfo  ( host , port ) ) ; }   public  List  < HostInfo > getDestinations  ( )  {  return  Collections . unmodifiableList  ( destinations ) ; }   public void setReattemptPrimaryConnectionDelay  (  Duration delay )  {  if  (   delay != null &&   delay . getMilliseconds  ( ) <= 0 )  {  throw  new IllegalArgumentException  ( "reattemptPrimaryConnectionDelay must be > 0" ) ; }    this . reattemptPrimaryConnectionDelay = delay ; }   public Duration getReattemptPrimaryConnectionDelay  ( )  {  return reattemptPrimaryConnectionDelay ; } }