  package    net . logstash . logback . appender ;   import   java . io . BufferedOutputStream ;  import   java . io . IOException ;  import   java . io . OutputStream ;  import   java . net . InetAddress ;  import   java . net . InetSocketAddress ;  import   java . net . Socket ;  import   java . net . UnknownHostException ;  import    java . nio . charset . Charset ;  import   java . util . ArrayList ;  import   java . util . Collections ;  import   java . util . Formatter ;  import   java . util . List ;  import    java . util . concurrent . BlockingQueue ;  import    java . util . concurrent . CountDownLatch ;  import    java . util . concurrent . RejectedExecutionException ;  import    java . util . concurrent . ScheduledFuture ;  import    java . util . concurrent . TimeUnit ;  import   javax . net . SocketFactory ;  import    javax . net . ssl . SSLContext ;  import    javax . net . ssl . SSLSocket ;  import    javax . net . ssl . SSLSocketFactory ;  import     net . logstash . logback . encoder . SeparatorParser ;  import      ch . qos . logback . core . encoder . Encoder ;  import       ch . qos . logback . core . net . ssl . ConfigurableSSLSocketFactory ;  import       ch . qos . logback . core . net . ssl . SSLConfigurableSocket ;  import       ch . qos . logback . core . net . ssl . SSLConfiguration ;  import       ch . qos . logback . core . net . ssl . SSLParametersConfiguration ;  import      ch . qos . logback . core . spi . DeferredProcessingAware ;  import      ch . qos . logback . core . status . ErrorStatus ;  import      ch . qos . logback . core . util . CloseUtil ;  import      ch . qos . logback . core . util . Duration ;  import    com . lmax . disruptor . EventHandler ;  import    com . lmax . disruptor . LifecycleAware ;  import    com . lmax . disruptor . RingBuffer ;  import   java . io . InputStream ;  import   java . net . SocketTimeoutException ;  import    java . util . concurrent . Future ;   public abstract class AbstractLogstashTcpSocketAppender  <  Event  extends DeferredProcessingAware >  extends  AsyncDisruptorAppender  < Event >  {   protected static final String  HOST_NAME_FORMAT = "%3$s" ;   protected static final String  PORT_FORMAT = "%4$d" ;   public static final String  DEFAULT_THREAD_NAME_FORMAT =        "logback-appender-" + APPENDER_NAME_FORMAT + "-" + HOST_NAME_FORMAT + ":" + PORT_FORMAT + "-" + THREAD_INDEX_FORMAT ;   public static final  int  DEFAULT_PORT = 4560 ;   public static final  int  DEFAULT_RECONNECTION_DELAY = 30000 ;   public static final  int  DEFAULT_QUEUE_SIZE = DEFAULT_RING_BUFFER_SIZE ;   public static final  int  DEFAULT_CONNECTION_TIMEOUT = 5000 ;   public static final  int  DEFAULT_WRITE_BUFFER_SIZE = 8192 ;   private static final  int  PRIMARY_DESTINATION_INDEX = 0 ;   private String  remoteHost ;   private  int  port = DEFAULT_PORT ;   private  List  < InetSocketAddress >  destinations =  new  ArrayList  < InetSocketAddress >  ( 2 ) ;   private volatile  int  connectedDestinationIndex = PRIMARY_DESTINATION_INDEX ;   private Duration  reconnectionDelay =  new Duration  ( DEFAULT_RECONNECTION_DELAY ) ;   private Duration  secondaryConnectionTTL ;   private  int  acceptConnectionTimeout = DEFAULT_CONNECTION_TIMEOUT ;   private String  peerId ;   private  Encoder  < Event >  encoder ;   private  int  writeBufferSize = DEFAULT_WRITE_BUFFER_SIZE ;   private SocketFactory  socketFactory ;   private SSLConfiguration  sslConfiguration ;   private Duration  keepAliveDuration ;   private String  keepAliveMessage =  System . getProperty  ( "line.separator" ) ;   private Charset  keepAliveCharset =  Charset . forName  ( "UTF-8" ) ;   private   byte  [ ]  keepAliveBytes ;   private volatile CountDownLatch  shutdownLatch ;   private class TcpSendingEventHandler  implements   EventHandler  <  LogEvent  < Event > > , LifecycleAware  {   private static final  int  MAX_REPEAT_CONNECTION_ERROR_LOG = 5 ;   private static final  int  MAX_REPEAT_WRITE_ATTEMPTS = 5 ;   private volatile Socket  socket ;   private volatile OutputStream  outputStream ;   private volatile  long  lastSentTimestamp ;   private volatile  long  secondaryConnectionExpirationTime =  Long . MAX_VALUE ;   private  ScheduledFuture  <  ? >  keepAliveFuture ;   private KeepAliveRunnable  keepAliveRunnable ;   private class KeepAliveRunnable  implements  Runnable  {   private  int  previousDestinationIndex = PRIMARY_DESTINATION_INDEX ;    @ Override public void run  ( )  {   long  lastSent = lastSentTimestamp ;   long  currentTime =  System . currentTimeMillis  ( ) ;  if  (  hasKeepAliveDurationElapsed  ( lastSent , currentTime ) )  {     getDisruptor  ( ) . getRingBuffer  ( ) . publishEvent  (  getEventTranslator  ( ) , null ) ;   scheduleKeepAlive  ( currentTime ) ; } else  {   scheduleKeepAlive  ( lastSent ) ; }  if  (  previousDestinationIndex != connectedDestinationIndex )  {   updateCurrentThreadName  ( ) ; }   previousDestinationIndex = connectedDestinationIndex ; } }    @ Override public void onEvent  (   LogEvent  < Event > logEvent ,   long sequence ,  boolean endOfBatch )  throws Exception  {  for (   int  i = 0 ;  i < MAX_REPEAT_WRITE_ATTEMPTS ;  i ++ )  {  if  (   this . socket == null )  {  return ; }  if  (  readerFuture . isDone  ( ) )  {   addInfo  (  peerId + "destination terminated the connection. Reconnecting." ) ;   reopenSocket  ( ) ;  continue ; }  try  {   long  currentTime =  System . currentTimeMillis  ( ) ;  if  (   logEvent . event != null )  {   encoder . doEncode  (  logEvent . event ) ; } else  if  (  hasKeepAliveDurationElapsed  ( lastSentTimestamp , currentTime ) )  {   outputStream . write  ( keepAliveBytes ) ; }  if  ( endOfBatch )  {   outputStream . flush  ( ) ; }   lastSentTimestamp = currentTime ;  if  (  shouldCloseConnection  ( currentTime ) )  {   addInfo  (  peerId + "closing connection and attempt to reconnect to primary server." ) ;   outputStream . flush  ( ) ;   reopenSocket  ( ) ; }  break ; }  catch (   Exception e )  {   addWarn  (    peerId + "unable to send event: " +  e . getMessage  ( ) + " Reconnecting." , e ) ;   reopenSocket  ( ) ; } } }   private boolean hasKeepAliveDurationElapsed  (   long lastSent ,   long currentTime )  {  return   isKeepAliveEnabled  ( ) &&   lastSent +  keepAliveDuration . getMilliseconds  ( ) < currentTime ; }   private boolean shouldCloseConnection  (   long currentTime )  {  return  secondaryConnectionExpirationTime <= currentTime ; }    @ Override public void onStart  ( )  {   openSocket  ( ) ;   scheduleKeepAlive  (  System . currentTimeMillis  ( ) ) ; }    @ Override public void onShutdown  ( )  {   unscheduleKeepAlive  ( ) ;   closeEncoder  ( ) ;   closeSocket  ( ) ; }   private synchronized void reopenSocket  ( )  {   closeSocket  ( ) ;   openSocket  ( ) ; }   private synchronized void openSocket  ( )  {   int  destinationIndex = 0 ;   int  errorCount = 0 ;  while  (   isStarted  ( ) &&  !   Thread . currentThread  ( ) . isInterrupted  ( ) )  {   long  startTime =  System . currentTimeMillis  ( ) ;  Socket  tempSocket = null ;  OutputStream  tempOutputStream = null ;  try  {  InetSocketAddress  currentDestination =   getDestinations  ( ) . get  ( destinationIndex ) ;   peerId =   "Log destination " + currentDestination + ": " ;   tempSocket =  socketFactory . createSocket  ( ) ;   tempSocket . setSoTimeout  ( acceptConnectionTimeout ) ;   tempSocket . connect  (  new InetSocketAddress  (  currentDestination . getHostString  ( ) ,  currentDestination . getPort  ( ) ) , acceptConnectionTimeout ) ;   tempOutputStream =  new BufferedOutputStream  (  tempSocket . getOutputStream  ( ) , writeBufferSize ) ;   encoder . init  ( tempOutputStream ) ;   addInfo  (  peerId + "connection established." ) ;    this . socket = tempSocket ;    this . outputStream = tempOutputStream ;  boolean  shouldUpdateThreadName =  (  destinationIndex != connectedDestinationIndex ) ;   
<<<<<<<
connectedDestinationIndex
=======
 this . readerFuture
>>>>>>>
 = 
<<<<<<<
destinationIndex
=======
  getExecutorService  ( ) . submit  (  new ReaderRunnable  (  tempSocket . getInputStream  ( ) ) )
>>>>>>>
 ;  if  (   secondaryConnectionTTL != null &&  destinationIndex != PRIMARY_DESTINATION_INDEX )  {   secondaryConnectionExpirationTime =  startTime +  secondaryConnectionTTL . getMilliseconds  ( ) ; } else  {   secondaryConnectionExpirationTime =  Long . MAX_VALUE ; }  if  ( shouldUpdateThreadName )  {   updateCurrentThreadName  ( ) ; }  return ; }  catch (   Exception e )  {   CloseUtil . closeQuietly  ( tempOutputStream ) ;   CloseUtil . closeQuietly  ( tempSocket ) ;   destinationIndex ++ ;  if  (  destinationIndex >=  destinations . size  ( ) )  {   destinationIndex = PRIMARY_DESTINATION_INDEX ;   long  sleepTime =  Math . max  ( 0 ,   reconnectionDelay . getMilliseconds  ( ) -  (   System . currentTimeMillis  ( ) - startTime ) ) ;  if  (   errorCount ++ <  MAX_REPEAT_CONNECTION_ERROR_LOG *  destinations . size  ( ) )  {   addWarn  (    peerId + "connection failed. Waiting " + sleepTime + "ms before attempting reconnection." , e ) ; }  if  (  sleepTime > 0 )  {  try  {   shutdownLatch . await  ( sleepTime ,  TimeUnit . MILLISECONDS ) ; }  catch (   InterruptedException ie )  {    Thread . currentThread  ( ) . interrupt  ( ) ;   addWarn  (  peerId + "connection interrupted. Will no longer attempt reconnection." ) ; } } } else  {  if  (   errorCount ++ <  MAX_REPEAT_CONNECTION_ERROR_LOG *  destinations . size  ( ) )  {   addWarn  (    peerId + "connection failed. Trying next server (" +   getDestinations  ( ) . get  ( destinationIndex ) + ")." , e ) ; } } } } }   private synchronized void closeSocket  ( )  {   CloseUtil . closeQuietly  ( outputStream ) ;   outputStream = null ;   CloseUtil . closeQuietly  ( socket ) ;   socket = null ;  if  (   this . readerFuture != null )  {    this . readerFuture . cancel  ( true ) ; } }   private void closeEncoder  ( )  {  try  {   encoder . close  ( ) ; }  catch (   IOException ioe )  {   addStatus  (  new ErrorStatus  (   "Failed to close encoder for appender named [" + name + "]." , this , ioe ) ) ; }   encoder . stop  ( ) ; }   private synchronized void scheduleKeepAlive  (   long basedOnTime )  {  if  (   isKeepAliveEnabled  ( ) &&  !   Thread . currentThread  ( ) . isInterrupted  ( ) )  {  if  (  keepAliveRunnable == null )  {   keepAliveRunnable =  new KeepAliveRunnable  ( ) ; }   long  delay =   keepAliveDuration . getMilliseconds  ( ) -  (   System . currentTimeMillis  ( ) - basedOnTime ) ;  try  {   keepAliveFuture =   getExecutorService  ( ) . schedule  ( keepAliveRunnable , delay ,  TimeUnit . MILLISECONDS ) ; }  catch (   RejectedExecutionException e )  {   keepAliveFuture = null ; } } }   private synchronized void unscheduleKeepAlive  ( )  {  if  (  keepAliveFuture != null )  {   keepAliveFuture . cancel  ( true ) ;  try  {   keepAliveFuture . get  ( ) ; }  catch (   InterruptedException e )  {    Thread . currentThread  ( ) . interrupt  ( ) ; }  catch (   Exception e )  { } } }   private  Future  <  ? >  readerFuture ;   private class ReaderRunnable  implements  Runnable  {   private final InputStream  inputStream ;   public ReaderRunnable  (  InputStream inputStream )  {  super  ( ) ;    this . inputStream = inputStream ; }    @ Override public void run  ( )  {  while  ( true )  {  try  {  if  (   inputStream . read  ( ) ==  - 1 )  {  break ; } }  catch (   SocketTimeoutException e )  { }  catch (   Exception e )  {  break ; } } } } }   private static class UnconnectedConfigurableSSLSocketFactory  extends ConfigurableSSLSocketFactory  {   private final SSLParametersConfiguration  parameters ;   private final SSLSocketFactory  delegate ;   public UnconnectedConfigurableSSLSocketFactory  (  SSLParametersConfiguration parameters ,  SSLSocketFactory delegate )  {  super  ( parameters , delegate ) ;    this . parameters = parameters ;    this . delegate = delegate ; }    @ Override public Socket createSocket  ( )  throws IOException  {  SSLSocket  socket =  ( SSLSocket )  delegate . createSocket  ( ) ;   parameters . configure  (  new SSLConfigurableSocket  ( socket ) ) ;  return socket ; } }   public AbstractLogstashTcpSocketAppender  ( )  {  super  ( ) ;   setEventHandler  (  new TcpSendingEventHandler  ( ) ) ;   setThreadNameFormat  ( DEFAULT_THREAD_NAME_FORMAT ) ; }    @ Override public boolean isStarted  ( )  {  CountDownLatch  latch =  this . shutdownLatch ;  return   latch != null &&   latch . getCount  ( ) != 0 ; }   public synchronized void start  ( )  {  if  (  isStarted  ( ) )  {  return ; }   int  errorCount = 0 ;  if  (  encoder == null )  {   errorCount ++ ;   addError  (   "No encoder was configured for appender " + name + "." ) ; }  if  (    destinations . size  ( ) > 0 &&  remoteHost != null )  {   errorCount ++ ;   addError  ( "Use '<remoteHost>/<port>' or '<destination>' but not both" ) ; }  if  (  remoteHost != null )  {   addWarn  ( "<remoteHost>/<port> are DEPRECATED, use <destination> instead" ) ;  try  {   addDestinations  (  InetSocketAddress . createUnresolved  ( remoteHost , port ) ) ; }  catch (   IllegalArgumentException e )  {   errorCount ++ ;   addError  (  e . getMessage  ( ) ) ; } }  if  (  destinations . isEmpty  ( ) )  {   errorCount ++ ;   addError  ( "No destination was configured. Use <destination> to add one or more destinations to the appender" ) ; }  if  (   errorCount == 0 &&  socketFactory == null )  {  if  (  sslConfiguration == null )  {   socketFactory =  SocketFactory . getDefault  ( ) ; } else  {  try  {  SSLContext  sslContext =   getSsl  ( ) . createContext  ( this ) ;  SSLParametersConfiguration  parameters =   getSsl  ( ) . getParameters  ( ) ;   parameters . setContext  (  getContext  ( ) ) ;   socketFactory =  new UnconnectedConfigurableSSLSocketFactory  ( parameters ,  sslContext . getSocketFactory  ( ) ) ; }  catch (   Exception e )  {   addError  ( "Unable to create ssl context" , e ) ;   errorCount ++ ; } } }  if  (   keepAliveMessage != null &&  keepAliveCharset != null )  {   keepAliveBytes =  keepAliveMessage . getBytes  ( keepAliveCharset ) ; }  if  (  errorCount == 0 )  {   encoder . setContext  (  getContext  ( ) ) ;  if  (  !  encoder . isStarted  ( ) )  {   encoder . start  ( ) ; }   int  threadPoolCoreSize =   getThreadPoolCoreSize  ( ) + 1 ;  if  (  keepAliveDuration != null )  {   threadPoolCoreSize ++ ; }   setThreadPoolCoreSize  ( threadPoolCoreSize ) ;    this . shutdownLatch =  new CountDownLatch  ( 1 ) ;   super . start  ( ) ; } }    @ Override public synchronized void stop  ( )  {  if  (  !  isStarted  ( ) )  {  return ; }    this . shutdownLatch . countDown  ( ) ;   super . stop  ( ) ; }   public  Encoder  < Event > getEncoder  ( )  {  return encoder ; }   public void setEncoder  (   Encoder  < Event > encoder )  {    this . encoder = encoder ; }   public SocketFactory getSocketFactory  ( )  {  return socketFactory ; }   public void setSocketFactory  (  SocketFactory socketFactory )  {    this . socketFactory = socketFactory ; }    @ Deprecated public void setRemoteHost  (  String host )  {   remoteHost = host ; }    @ Deprecated public String getRemoteHost  ( )  {  return remoteHost ; }    @ Deprecated public void setPort  (   int port )  {    this . port = port ; }    @ Deprecated public  int getPort  ( )  {  return port ; }   public void addDestination  (   final String destination )  throws IllegalArgumentException  {   List  < InetSocketAddress >  parsedDestinations =  DestinationParser . parse  ( destination , DEFAULT_PORT ) ;   addDestinations  (  parsedDestinations . toArray  (  new InetSocketAddress  [  parsedDestinations . size  ( ) ] ) ) ; }   public void addDestinations  (  InetSocketAddress ...  destinations )  throws IllegalArgumentException  {  if  (  destinations == null )  {  return ; }  for ( InetSocketAddress destination : destinations )  {  try  {   InetAddress . getByName  (  destination . getHostString  ( ) ) ; }  catch (   UnknownHostException ex )  {   addWarn  (     "Invalid destination '" +  destination . getHostString  ( ) + "': host unknown (was '" +  destination . getHostString  ( ) + "')." , ex ) ; }    this . destinations . add  ( destination ) ; } }   protected void updateCurrentThreadName  ( )  {    Thread . currentThread  ( ) . setName  (  calculateThreadName  ( ) ) ; }    @ Override protected  List  < Object > getThreadNameFormatParams  ( )  {   List  < Object >  superThreadNameFormatParams =  super . getThreadNameFormatParams  ( ) ;   List  < Object >  threadNameFormatParams =  new  ArrayList  < Object >  (   superThreadNameFormatParams . size  ( ) + 2 ) ;   threadNameFormatParams . addAll  ( superThreadNameFormatParams ) ;  InetSocketAddress  currentDestination =   this . destinations . get  ( connectedDestinationIndex ) ;   threadNameFormatParams . add  (  currentDestination . getHostString  ( ) ) ;   threadNameFormatParams . add  (  currentDestination . getPort  ( ) ) ;  return threadNameFormatParams ; }   public  List  < InetSocketAddress > getDestinations  ( )  {  return  Collections . unmodifiableList  ( destinations ) ; }   public void setReconnectionDelay  (  Duration delay )  {  if  (   delay == null ||   delay . getMilliseconds  ( ) <= 0 )  {  throw  new IllegalArgumentException  ( "reconnectionDelay must be > 0" ) ; }    this . reconnectionDelay = delay ; }   public Duration getReconnectionDelay  ( )  {  return reconnectionDelay ; }   public void setSecondaryConnectionTTL  (  Duration secondaryConnectionTTL )  {  if  (   secondaryConnectionTTL != null &&   secondaryConnectionTTL . getMilliseconds  ( ) <= 0 )  {  throw  new IllegalArgumentException  ( "secondaryConnectionTTL must be > 0" ) ; }    this . secondaryConnectionTTL = secondaryConnectionTTL ; }   public Duration getSecondaryConnectionTTL  ( )  {  return secondaryConnectionTTL ; }  void setAcceptConnectionTimeout  (   int acceptConnectionTimeout )  {    this . acceptConnectionTimeout = acceptConnectionTimeout ; }   public  int getWriteBufferSize  ( )  {  return writeBufferSize ; }   public void setWriteBufferSize  (   int writeBufferSize )  {    this . writeBufferSize = writeBufferSize ; }   public  int getQueueSize  ( )  {  return  getRingBufferSize  ( ) ; }   public void setQueueSize  (   int queueSize )  {   setRingBufferSize  ( queueSize ) ; }   public SSLConfiguration getSsl  ( )  {  return sslConfiguration ; }   public void setSsl  (  SSLConfiguration sslConfiguration )  {    this . sslConfiguration = sslConfiguration ; }   public Duration getKeepAliveDuration  ( )  {  return keepAliveDuration ; }   public void setKeepAliveDuration  (  Duration keepAliveDuration )  {    this . keepAliveDuration = keepAliveDuration ; }   public String getKeepAliveMessage  ( )  {  return keepAliveMessage ; }   public void setKeepAliveMessage  (  String keepAliveMessage )  {    this . keepAliveMessage =  SeparatorParser . parseSeparator  ( keepAliveMessage ) ; }   public boolean isKeepAliveEnabled  ( )  {  return    this . keepAliveDuration != null &&   this . keepAliveMessage != null ; }   public Charset getKeepAliveCharset  ( )  {  return keepAliveCharset ; }   public void setKeepAliveCharset  (  Charset keepAliveCharset )  {    this . keepAliveCharset = keepAliveCharset ; }    @ Override public void setThreadNameFormat  (  String threadNameFormat )  {   super . setThreadNameFormat  ( threadNameFormat ) ; } }