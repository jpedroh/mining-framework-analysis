  package      com . spotify . netty . handler . codec . zmtp ;   import    io . netty . buffer . ByteBuf ;  import   java . util . ArrayList ;  import   java . util . List ;  import static        com . spotify . netty . handler . codec . zmtp . ZMTPUtils . MORE_FLAG ;  import static    java . lang . Math . min ;  import static    java . nio . ByteOrder . BIG_ENDIAN ;  import static      org . jboss . netty . buffer . ChannelBuffers . swapLong ;   public class ZMTPMessageParser  {   private final boolean  enveloped ;   private final  long  sizeLimit ;   private  List  < ZMTPFrame >  envelope ;   private  List  < ZMTPFrame >  content ;   private  List  < ZMTPFrame >  part ;   private boolean  hasMore ;   private  long  size ;   private  int  frameSize ;   private  int  frameRemaining ;   private boolean  headerParsed ;   public ZMTPParsedMessage parse  (   final ByteBuf buffer )  throws ZMTPMessageParsingException  {  if  (  isOversized  ( size ) )  {  return  discardFrames  ( buffer ) ; }  while  (   buffer . readableBytes  ( ) > 0 )  {   buffer . markReaderIndex  ( ) ;   final boolean  parsedHeader =  parseFrameHeader  ( buffer ) ;  if  (  ! parsedHeader )  {   buffer . resetReaderIndex  ( ) ;  return null ; }  if  (  isOversized  (  size + frameSize ) )  {   buffer . resetReaderIndex  ( ) ;  return  discardFrames  ( buffer ) ; }  if  (  frameSize >  buffer . readableBytes  ( ) )  {   buffer . resetReaderIndex  ( ) ;  return null ; }   size += frameSize ;   final ZMTPFrame  frame =  ZMTPFrame . read  ( buffer , frameSize ) ;  if  (   !  frame . hasData  ( ) &&  part == envelope )  {   part = content ; } else  {   part . add  ( frame ) ; }  if  (  ! hasMore )  {  return  finish  ( false ) ; } }  return null ; }   private boolean isOversized  (   final  long size )  {  return  size > sizeLimit ; }   private ZMTPParsedMessage finish  (   final boolean truncated )  {   final ZMTPMessage  message =  new ZMTPMessage  ( envelope , content ) ;   final ZMTPParsedMessage  parsedMessage =  new ZMTPParsedMessage  ( truncated , size , message ) ;   reset  ( ) ;  return parsedMessage ; }   private void reset  ( )  {   envelope =  new  ArrayList  < ZMTPFrame >  ( 3 ) ;   content =  new  ArrayList  < ZMTPFrame >  ( 3 ) ;   part =  enveloped ? envelope : content ;   hasMore = true ;   size = 0 ; }   private ZMTPParsedMessage discardFrames  (   final ByteBuf buffer )  throws ZMTPMessageParsingException  {  while  (   buffer . readableBytes  ( ) > 0 )  {  if  (  ! headerParsed )  {   buffer . markReaderIndex  ( ) ;   headerParsed =  parseFrameHeader  ( buffer ) ;  if  (  ! headerParsed )  {   buffer . resetReaderIndex  ( ) ;  return null ; }   size += frameSize ;   frameRemaining = frameSize ; }   final  int  discardBytes =  min  ( frameRemaining ,  buffer . readableBytes  ( ) ) ;   frameRemaining -= discardBytes ;   buffer . skipBytes  ( discardBytes ) ;   final boolean  done =  frameRemaining == 0 ;  if  ( done )  {   headerParsed = false ; }  if  (  done &&  ! hasMore )  {  return  finish  ( true ) ; } }  return null ; }   private boolean parseFrameHeader  (   final ByteBuf buffer )  throws ZMTPMessageParsingException  {   final  long  len =  ZMTPUtils . decodeLength  ( buffer ) ;  if  (  len >  Integer . MAX_VALUE )  {  throw  new ZMTPMessageParsingException  (  "Received too large frame: " + len ) ; }  if  (  len ==  - 1 )  {  return false ; }  if  (  len == 0 )  {  throw  new ZMTPMessageParsingException  ( "Received frame with zero length" ) ; }  if  (   buffer . readableBytes  ( ) < 1 )  {  return false ; }   frameSize =   (  int ) len - 1 ;   hasMore =   (   buffer . readByte  ( ) & MORE_FLAG ) == MORE_FLAG ;  return true ; }   private static final  byte  LONG_FLAG = 0x02 ;   private final  int  version ;   public ZMTPMessageParser  (   final boolean enveloped ,   final  long sizeLimit ,   int version )  {    this . enveloped = enveloped ;    this . sizeLimit = sizeLimit ;    this . version = version ;   reset  ( ) ; } 
<<<<<<<
=======
  public ZMTPParsedMessage parse  (   final ChannelBuffer buffer )  throws ZMTPMessageParsingException  {  if  (  isOversized  ( size ) )  {  return  discardFrames  ( buffer ) ; }  while  (   buffer . readableBytes  ( ) > 0 )  {   buffer . markReaderIndex  ( ) ;   final boolean  parsedHeader =  parseZMTPHeader  ( buffer ) ;  if  (  ! parsedHeader )  {   buffer . resetReaderIndex  ( ) ;  return null ; }  if  (  isOversized  (  size + frameSize ) )  {   buffer . resetReaderIndex  ( ) ;  return  discardFrames  ( buffer ) ; }  if  (  frameSize >  buffer . readableBytes  ( ) )  {   buffer . resetReaderIndex  ( ) ;  return null ; }   size += frameSize ;   final ZMTPFrame  frame =  ZMTPFrame . read  ( buffer , frameSize ) ;  if  (   !  frame . hasData  ( ) &&  part == envelope )  {   part = content ; } else  {   part . add  ( frame ) ; }  if  (  ! hasMore )  {  return  finish  ( false ) ; } }  return null ; }
>>>>>>>
 
<<<<<<<
=======
  private ZMTPParsedMessage discardFrames  (   final ChannelBuffer buffer )  throws ZMTPMessageParsingException  {  while  (   buffer . readableBytes  ( ) > 0 )  {  if  (  ! headerParsed )  {   buffer . markReaderIndex  ( ) ;   headerParsed =  parseZMTPHeader  ( buffer ) ;  if  (  ! headerParsed )  {   buffer . resetReaderIndex  ( ) ;  return null ; }   size += frameSize ;   frameRemaining = frameSize ; }   final  int  discardBytes =  min  ( frameRemaining ,  buffer . readableBytes  ( ) ) ;   frameRemaining -= discardBytes ;   buffer . skipBytes  ( discardBytes ) ;   final boolean  done =  frameRemaining == 0 ;  if  ( done )  {   headerParsed = false ; }  if  (  done &&  ! hasMore )  {  return  finish  ( true ) ; } }  return null ; }
>>>>>>>
   private boolean parseZMTPHeader  (   final ChannelBuffer buffer )  throws ZMTPMessageParsingException  {  return   version == 1 ?  parseZMTP1Header  ( buffer ) :  parseZMTP2Header  ( buffer ) ; }   private boolean parseZMTP1Header  (   final ChannelBuffer buffer )  throws ZMTPMessageParsingException  {   final  long  len =  ZMTPUtils . decodeLength  ( buffer ) ;  if  (  len >  Integer . MAX_VALUE )  {  throw  new ZMTPMessageParsingException  (  "Received too large frame: " + len ) ; }  if  (  len ==  - 1 )  {  return false ; }  if  (  len == 0 )  {  throw  new ZMTPMessageParsingException  ( "Received frame with zero length" ) ; }  if  (   buffer . readableBytes  ( ) < 1 )  {  return false ; }   frameSize =   (  int ) len - 1 ;   hasMore =   (   buffer . readByte  ( ) & MORE_FLAG ) == MORE_FLAG ;  return true ; }   private boolean parseZMTP2Header  (  ChannelBuffer buffer )  throws ZMTPMessageParsingException  {  if  (   buffer . readableBytes  ( ) < 2 )  {  return false ; }   int  flags =  buffer . readByte  ( ) ;   hasMore =   (  flags & MORE_FLAG ) == MORE_FLAG ;  if  (   (  flags & LONG_FLAG ) != LONG_FLAG )  {   frameSize =   buffer . readByte  ( ) & 0xff ;  return true ; }  if  (   buffer . readableBytes  ( ) < 8 )  {  return false ; }   long  len ;  if  (   buffer . order  ( ) == BIG_ENDIAN )  {   len =  buffer . readLong  ( ) ; } else  {   len =  swapLong  (  buffer . readLong  ( ) ) ; }  if  (  len >  Integer . MAX_VALUE )  {  throw  new ZMTPMessageParsingException  (  "Received too large frame: " + len ) ; }   frameSize =  (  int ) len ;  return true ; } }