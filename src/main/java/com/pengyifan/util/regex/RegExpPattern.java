  package    com . pengyifan . util . regex ;   import     com . google . common . base . Joiner ;  import     com . google . common . collect . Lists ;  import     com . google . common . collect . Sets ;  import     org . apache . commons . io . FileUtils ;  import   java . io . IOException ;  import    java . nio . file . Path ;  import   java . util . Iterator ;  import   java . util . LinkedList ;  import   java . util . List ;  import   java . util . Optional ;  import   java . util . Set ;  import   java . util . Stack ;  import   java . util . StringJoiner ;  import    java . util . regex . PatternSyntaxException ;  import    java . util . stream . Collectors ;   public class RegExpPattern  {   private final static  char  epsilon = 0 ;   private final static RegInfixToPostfix  converter =  new RegInfixToPostfix  ( ) ;   private final FsaTable  nfaTable ;   private FsaTable  dfaTable ;   private final  Stack  < FsaTable >  operandStack ;   private final  Stack  < Character >  operatorStack ;   private  int  nextStateID ;   private final  Set  < Character >  inputSet ;   private final String  pattern ;   private RegExpPattern  (  String pattern )  {   nextStateID = 0 ;   nfaTable =  new FsaTable  ( ) ;   dfaTable =  new FsaTable  ( ) ;   operandStack =  new  Stack  < >  ( ) ;   operatorStack =  new  Stack  < >  ( ) ;   inputSet =  Sets . newHashSet  ( ) ;    this . pattern = pattern ;  if  (  !  compile  ( ) )  {  throw  new PatternSyntaxException  ( "Illegal pattern" , pattern , 0 ) ; } }   public static RegExpPattern compile  (  String regex )  {  return  new RegExpPattern  ( regex ) ; }   public RegExpMatcher matcher  (  CharSequence input )  {  return  new RegExpMatcher  ( this , input ) ; }   protected FsaTable getDfaTable  ( )  {  return dfaTable ; }   private void push  (   char ch )  {  RegExpState  s0 =  new RegExpState  (  ++ nextStateID ) ;  RegExpState  s1 =  new RegExpState  (  ++ nextStateID ) ;   s0 . addTransition  ( ch , s1 ) ;  FsaTable  NFATable =  new FsaTable  ( ) ;   NFATable . add  ( s0 ) ;   NFATable . add  ( s1 ) ;   operandStack . push  ( NFATable ) ;   inputSet . add  ( ch ) ; }   private FsaTable pop  ( )  {  FsaTable  table =  new FsaTable  ( ) ;  if  (  !  operandStack . isEmpty  ( ) )  {   table . addAll  (  operandStack . pop  ( ) ) ; }  return table ; }   private boolean concat  ( )  {  FsaTable  B =  pop  ( ) ;  FsaTable  A =  pop  ( ) ;  if  (   B . isEmpty  ( ) ||  A . isEmpty  ( ) )  {  return false ; }    A . getLast  ( ) . addTransition  ( epsilon ,  B . getFirst  ( ) ) ;   A . addAll  ( B ) ;   operandStack . push  ( A ) ;  return true ; }   private boolean star  ( )  {  FsaTable  A =  pop  ( ) ;  if  (  A . isEmpty  ( ) )  {  return false ; }  RegExpState  pStartState =  new RegExpState  (  ++ nextStateID ) ;  RegExpState  pEndState =  new RegExpState  (  ++ nextStateID ) ;   pStartState . addTransition  ( epsilon , pEndState ) ;   pStartState . addTransition  ( epsilon ,  A . getFirst  ( ) ) ;    A . getLast  ( ) . addTransition  ( epsilon , pEndState ) ;    A . getLast  ( ) . addTransition  ( epsilon ,  A . getFirst  ( ) ) ;   A . add  ( pEndState ) ;   A . addFirst  ( pStartState ) ;   operandStack . push  ( A ) ;    System . out . printf  ( "STAR\n" ) ;  return true ; }   private boolean union  ( )  {  FsaTable  B =  pop  ( ) ;  FsaTable  A =  pop  ( ) ;  if  (   B . isEmpty  ( ) ||  A . isEmpty  ( ) )  {  return false ; }  RegExpState  pStartState =  new RegExpState  (  ++ nextStateID ) ;  RegExpState  pEndState =  new RegExpState  (  ++ nextStateID ) ;   pStartState . addTransition  ( epsilon ,  A . getFirst  ( ) ) ;   pStartState . addTransition  ( epsilon ,  B . getFirst  ( ) ) ;    A . getLast  ( ) . addTransition  ( epsilon , pEndState ) ;    B . getLast  ( ) . addTransition  ( epsilon , pEndState ) ;   B . add  ( pEndState ) ;   A . addFirst  ( pStartState ) ;   A . addAll  ( B ) ;   operandStack . push  ( A ) ;  return true ; }   private boolean createNfa  ( )  {   List  < Character >  postfix =  converter . convertToPostfix  ( pattern ) ;  for (  char c : postfix )  {  if  (  !  RegInfixToPostfix . isOperator  ( c ) )  {   push  ( c ) ; } else  {  switch  ( c )  {   case '*' :   star  ( ) ;  break ;   case '|' :   union  ( ) ;  break ;   case  RegInfixToPostfix . CONTACT_CHAR :   concat  ( ) ;  break ;   default :  return false ; } } }  FsaTable  table =  pop  ( ) ;  if  (  table . isEmpty  ( ) )  {  return false ; }   nfaTable . addAll  ( table ) ;    nfaTable . getLast  ( ) . setIsAcceptingState  ( true ) ;  return true ; }   private  Set  < RegExpState > epsilonClosure  (   Set  < RegExpState > T )  {   Set  < RegExpState >  Res =  Sets . newHashSet  ( T ) ;   Stack  < RegExpState >  unprocessedStack =  new  Stack  < >  ( ) ;  for ( RegExpState state : T )  {   unprocessedStack . push  ( state ) ; }  while  (  !  unprocessedStack . isEmpty  ( ) )  {  RegExpState  t =  unprocessedStack . pop  ( ) ;  for ( RegExpState u :  t . getTransition  ( epsilon ) )  {  if  (  !  Res . contains  ( u ) )  {   Res . add  ( u ) ;   unprocessedStack . push  ( u ) ; } } }  return Res ; }   private  Set  < RegExpState > move  (   char ch ,   Set  < RegExpState > T )  {  return     T . stream  ( ) . map  (  s ->  s . getTransition  ( ch ) ) . flatMap  (  List :: stream ) . collect  (  Collectors . toSet  ( ) ) ; }   private void convertNfaToDfa  ( )  {   dfaTable . clear  ( ) ;  if  (  nfaTable . isEmpty  ( ) )  {  return ; }   nextStateID = 0 ;   LinkedList  < RegExpState >  unmarkedStates =  Lists . newLinkedList  ( ) ;   Set  < RegExpState >  NFAStartStateSet =  Sets . newHashSet  (  nfaTable . getFirst  ( ) ) ;   Set  < RegExpState >  DFAStartStateSet =  epsilonClosure  ( NFAStartStateSet ) ;  RegExpState  DFAStartState =  new RegExpState  ( DFAStartStateSet ,  ++ nextStateID ) ;   dfaTable . add  ( DFAStartState ) ;   unmarkedStates . add  ( DFAStartState ) ;  while  (  !  unmarkedStates . isEmpty  ( ) )  {  RegExpState  processingDFAState =  unmarkedStates . removeLast  ( ) ;  for (  char c : inputSet )  {   Set  < RegExpState >  EpsilonClosureRes =  epsilonClosure  (  move  ( c ,  processingDFAState . getNfaStates  ( ) ) ) ;  if  (  EpsilonClosureRes . isEmpty  ( ) )  {  continue ; }   Optional  < RegExpState >  opt =    dfaTable . stream  ( ) . filter  (  s ->   s . getNfaStates  ( ) . equals  ( EpsilonClosureRes ) ) . findFirst  ( ) ;  if  (  opt . isPresent  ( ) )  {   processingDFAState . addTransition  ( c ,  opt . get  ( ) ) ; } else  {  RegExpState  U =  new RegExpState  ( EpsilonClosureRes ,  ++ nextStateID ) ;   unmarkedStates . add  ( U ) ;   dfaTable . add  ( U ) ;   processingDFAState . addTransition  ( c , U ) ; } } } }   private void reduceDfa  ( )  {   Set  < RegExpState >  DeadEndSet =    dfaTable . stream  ( ) . filter  (  RegExpState :: isDeadEnd ) . collect  (  Collectors . toSet  ( ) ) ;  if  (  DeadEndSet . isEmpty  ( ) )  {  return ; }  for ( RegExpState state : DeadEndSet )  {   dfaTable . forEach  (  s ->  s . removeTransition  ( state ) ) ;   Iterator  < RegExpState >  itr =  dfaTable . iterator  ( ) ;  while  (  itr . hasNext  ( ) )  {  RegExpState  s =  itr . next  ( ) ;  if  (  s . equals  ( state ) )  {   itr . remove  ( ) ; } } } }   private void reset  ( )  {   nfaTable . clear  ( ) ;   dfaTable . clear  ( ) ;   nextStateID = 0 ;   operandStack . clear  ( ) ;   operatorStack . clear  ( ) ;   inputSet . clear  ( ) ; }   private boolean compile  ( )  {   reset  ( ) ;  if  (  !  createNfa  ( ) )  {  return false ; }   convertNfaToDfa  ( ) ;   reduceDfa  ( ) ;  return true ; }   public void writeNfaTable  (  Path file )  throws IOException  {  StringBuilder  strNFATable =  new StringBuilder  ( ) ;  for (  char c : inputSet )  {    strNFATable . append  ( "\t\t" ) . append  ( c ) ; }    strNFATable . append  ( "\t\tepsilon" ) . append  ( '\n' ) ;  for ( RegExpState pState : nfaTable )  {   strNFATable . append  (  pState . getStateID  ( ) ) ;  for (  char c : inputSet )  {    strNFATable . append  ( "\t\t" ) . append  (  getStateString  (  pState . getTransition  ( c ) ) ) ; }     strNFATable . append  ( "\t\t" ) . append  (  getStateString  (  pState . getTransition  ( epsilon ) ) ) . append  ( '\n' ) ; }   FileUtils . writeStringToFile  (  file . toFile  ( ) ,  strNFATable . toString  ( ) ) ; }   private String getStateString  (   List  < RegExpState > states )  {  StringJoiner  sj =  new StringJoiner  ( "," ) ;   states . forEach  (  s ->  sj . add  (  String . valueOf  (  s . getStateID  ( ) ) ) ) ;  return  sj . toString  ( ) ; }   public void writeDfaTable  (  Path file )  throws IOException  {  StringBuilder  strDFATable =  new StringBuilder  ( ) ;     strDFATable . append  ( "\t\t" ) . append  (   Joiner . on  ( "\t\t" ) . join  ( inputSet ) ) . append  ( '\n' ) ;  for ( RegExpState state : dfaTable )  {   strDFATable . append  (  state . getStateID  ( ) ) ;  for (  char c : inputSet )  {    strDFATable . append  ( "\t\t" ) . append  (  getStateString  (  state . getTransition  ( c ) ) ) ; }   strDFATable . append  ( '\n' ) ; }   FileUtils . writeStringToFile  (  file . toFile  ( ) ,  strDFATable . toString  ( ) ) ; }   public void writeNfaGraph  (  Path file )  throws IOException  {   FileUtils . writeStringToFile  (  file . toFile  ( ) ,  toDOT  ( nfaTable ) ) ; }   public void writeDfaGraph  (  Path file )  throws IOException  {   FileUtils . writeStringToFile  (  file . toFile  ( ) ,  toDOT  ( dfaTable ) ) ; }   private String toDOT  (  FsaTable table )  {  StringBuilder  strDFAGraph =  new StringBuilder  ( "digraph{\n" ) ;     table . stream  ( ) . filter  (  RegExpState :: isAcceptingState ) . forEach  (  s ->    strDFAGraph . append  ( '\t' ) . append  (  s . getStateID  ( ) ) . append  ( "\t[shape=doublecircle];\n" ) ) ;   strDFAGraph . append  ( '\n' ) ;  for ( RegExpState s1 : table )  {  for ( RegExpState s2 :  s1 . getTransition  ( epsilon ) )  {       strDFAGraph . append  ( '\t' ) . append  (  s1 . getStateID  ( ) ) . append  ( " -> " ) . append  (  s2 . getStateID  ( ) ) . append  ( "\t[label=\"epsilon\"];\n" ) ; }  for (  char c : inputSet )  {  for ( RegExpState s2 :  s1 . getTransition  ( c ) )  {         strDFAGraph . append  ( '\t' ) . append  (  s1 . getStateID  ( ) ) . append  ( " -> " ) . append  (  s2 . getStateID  ( ) ) . append  ( "\t[label=\"" ) . append  ( c ) . append  ( "\"];\n" ) ; } } }   strDFAGraph . append  ( '}' ) ;  return  strDFAGraph . toString  ( ) ; }   static class FsaTable  extends  LinkedList  < RegExpState >  { } }