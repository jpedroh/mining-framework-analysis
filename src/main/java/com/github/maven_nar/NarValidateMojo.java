  package   com . github . maven_nar ;   import   java . io . File ;  import   java . util . List ;  import     org . apache . maven . plugin . MojoExecutionException ;  import     org . apache . maven . plugin . MojoFailureException ;  import      org . apache . maven . plugins . annotations . LifecyclePhase ;  import      org . apache . maven . plugins . annotations . Mojo ;  import      org . apache . maven . plugins . annotations . Parameter ;    @ Mojo  (  name = "nar-validate" ,  defaultPhase =  LifecyclePhase . VALIDATE ) public class NarValidateMojo  extends AbstractCompileMojo  {    @ Parameter  (  defaultValue = "${basedir}/src/gnu" ,  required = true ) private File  gnuSourceDirectory ;    @ Override protected List getArtifacts  ( )  {  return null ; }    @ Override public final void narExecute  ( )  throws MojoExecutionException , MojoFailureException  {   final AOL  aol =  getAOL  ( ) ;    getLog  ( ) . info  (  "Using AOL: " + aol ) ;   final Linker  linker =  getLinker  ( ) ;    getLog  ( ) . debug  (  "Using linker version: " +  linker . getVersion  ( this ) ) ;   int  noOfCompilers = 0 ;  if  (  this . onlySpecifiedCompilers )  {  if  (    getCpp  ( ) != null &&    getCpp  ( ) . getName  ( ) != null )  {   noOfCompilers ++ ;  if  (    getCpp  ( ) . getIncludes  (  Compiler . MAIN ) . isEmpty  ( ) )  {  throw  new MojoExecutionException  (  "No includes defined for compiler " +   getCpp  ( ) . getName  ( ) ) ; } }  if  (    getC  ( ) != null &&    getC  ( ) . getName  ( ) != null )  {   noOfCompilers ++ ;  if  (    getC  ( ) . getIncludes  (  Compiler . MAIN ) . isEmpty  ( ) )  {  throw  new MojoExecutionException  (  "No includes defined for compiler " +   getC  ( ) . getName  ( ) ) ; } }  if  (    getFortran  ( ) != null &&    getFortran  ( ) . getName  ( ) != null )  {   noOfCompilers ++ ;  if  (    getFortran  ( ) . getIncludes  (  Compiler . MAIN ) . isEmpty  ( ) )  {  throw  new MojoExecutionException  (  "No includes defined for compiler " +   getFortran  ( ) . getName  ( ) ) ; } }  if  (   noOfCompilers == 0 &&  (    this . gnuSourceDirectory == null ||  !   this . gnuSourceDirectory . exists  ( ) ) )  {  throw  new MojoExecutionException  (    "No compilers defined for linker " +  linker . getName  ( ) + ", and no" + " <gnuSourceDirectory> is defined.  Either define a compiler or a linker." ) ; } } } }