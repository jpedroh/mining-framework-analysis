  package   com . github . maven_nar ;   import   java . io . File ;  import   java . io . IOException ;  import   java . util . Iterator ;  import   java . util . List ;  import     org . apache . maven . artifact . Artifact ;  import     org . apache . maven . plugin . MojoExecutionException ;  import     org . apache . maven . plugin . MojoFailureException ;  import     org . codehaus . plexus . util . FileUtils ;  import      org . apache . maven . plugins . annotations . LifecyclePhase ;  import      org . apache . maven . plugins . annotations . Mojo ;  import      org . apache . maven . plugins . annotations . ResolutionScope ;   public  @ Mojo  (  name = "nar-assembly" ,  defaultPhase =  LifecyclePhase . PROCESS_RESOURCES ,  requiresProject = true ,  requiresDependencyResolution =  ResolutionScope . COMPILE ) class NarAssemblyMojo  extends AbstractDependencyMojo  {    @ Override protected List getArtifacts  ( )  {  return   getMavenProject  ( ) . getCompileArtifacts  ( ) ; }   public final void narExecute  ( )  throws MojoExecutionException , MojoFailureException  {   List  < NarArtifact >  narArtifacts =  getNarArtifacts  ( ) ;   List  < AttachedNarArtifact >  dependencies =  getAllAttachedNarArtifacts  ( narArtifacts ) ;   downloadAttachedNars  ( dependencies ) ;   unpackAttachedNars  ( dependencies ) ;  for (  Iterator  d =  dependencies . iterator  ( ) ;  d . hasNext  ( ) ; )  {  Artifact  dependency =  ( Artifact )  d . next  ( ) ;    getLog  ( ) . debug  (  "Assemble from " + dependency ) ;   dependency . isSnapshot  ( ) ;  File  srcDir =   getLayout  ( ) . getNarUnpackDirectory  (  getUnpackDirectory  ( ) ,   getNarManager  ( ) . getNarFile  ( dependency ) ) ;  File  dstDir =  getTargetDirectory  ( ) ;  try  {   FileUtils . mkdir  (  dstDir . getPath  ( ) ) ;    getLog  ( ) . debug  (  "SrcDir: " + srcDir ) ;  if  (  srcDir . exists  ( ) )  {   FileUtils . copyDirectoryStructureIfModified  ( srcDir , dstDir ) ; } }  catch (   IOException ioe )  {  throw  new MojoExecutionException  (      "Failed to copy directory for dependency " + dependency + " from " + srcDir + " to " + dstDir , ioe ) ; } } } }