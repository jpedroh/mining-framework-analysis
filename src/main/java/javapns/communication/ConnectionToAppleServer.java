  package  javapns . communication ;   import    javapns . communication . exceptions . CommunicationException ;  import    javapns . communication . exceptions . KeystoreException ;  import   org . slf4j . Logger ;  import   org . slf4j . LoggerFactory ;  import   javax . net . ssl .  * ;  import   java . io . IOException ;  import   java . io . InputStream ;  import   java . io . OutputStream ;  import   java . io . UnsupportedEncodingException ;  import   java . net . Socket ;  import   java . security . KeyStore ;   public abstract class ConnectionToAppleServer  {   public static final String  KEYSTORE_TYPE_PKCS12 = "PKCS12" ;   public static final String  KEYSTORE_TYPE_JKS = "JKS" ;   private static final Logger  logger =  LoggerFactory . getLogger  (  ConnectionToAppleServer . class ) ;   private static final String  ALGORITHM =  KeyManagerFactory . getDefaultAlgorithm  ( ) ;   private static final String  PROTOCOL = "TLS" ;   private final AppleServer  server ;   private KeyStore  keyStore ;   private SSLSocketFactory  socketFactory ;   protected ConnectionToAppleServer  (   final AppleServer server )  throws KeystoreException  {    this . server = server ;    this . keyStore =  KeystoreManager . loadKeystore  ( server ) ; }   protected ConnectionToAppleServer  (   final AppleServer server ,   final KeyStore keystore )  {    this . server = server ;    this . keyStore = keystore ; }   public AppleServer getServer  ( )  {  return server ; }   private KeyStore getKeystore  ( )  {  return keyStore ; }   public void setKeystore  (   final KeyStore ks )  {    this . keyStore = ks ; }   private SSLSocketFactory createSSLSocketFactoryWithTrustManagers  (   final  TrustManager  [ ] trustManagers )  throws KeystoreException  {   logger . debug  ( "Creating SSLSocketFactory" ) ;  try  {   final KeyStore  keystore =  getKeystore  ( ) ;   final KeyManagerFactory  kmf =  KeyManagerFactory . getInstance  ( ALGORITHM ) ;  try  {   final   char  [ ]  password =  KeystoreManager . getKeystorePasswordForSSL  ( server ) ;   kmf . init  ( keystore , password ) ; }  catch (   Exception e )  {   final KeystoreException  wrappedKeystoreException =  KeystoreManager . wrapKeystoreException  ( e ) ;  throw wrappedKeystoreException ; }   final SSLContext  sslc =  SSLContext . getInstance  ( PROTOCOL ) ;   sslc . init  (  kmf . getKeyManagers  ( ) , trustManagers , null ) ;  return  sslc . getSocketFactory  ( ) ; }  catch (   final  Exception e )  {  throw  new KeystoreException  (  "Keystore exception: " +  e . getMessage  ( ) , e ) ; } }   public abstract String getServerHost  ( ) ;   protected abstract  int getServerPort  ( ) ;   private SSLSocketFactory createSSLSocketFactory  ( )  throws KeystoreException  {  return  createSSLSocketFactoryWithTrustManagers  (  new TrustManager  [ ]  {  new ServerTrustingTrustManager  ( ) } ) ; }   private SSLSocketFactory getSSLSocketFactory  ( )  throws KeystoreException  {  if  (  socketFactory == null )  {   socketFactory =  createSSLSocketFactory  ( ) ; }  return socketFactory ; }   public SSLSocket getSSLSocket  ( )  throws KeystoreException , CommunicationException  {   final SSLSocketFactory  sslSocketFactory =  getSSLSocketFactory  ( ) ;   logger . debug  (    "Creating SSLSocket to " +  getServerHost  ( ) + ":" +  getServerPort  ( ) ) ;  try  {  if  (  ProxyManager . isUsingProxy  ( server ) )  {  return  tunnelThroughProxy  ( sslSocketFactory ) ; } else  {  return  ( SSLSocket )  sslSocketFactory . createSocket  (  getServerHost  ( ) ,  getServerPort  ( ) ) ; } }  catch (   final  Exception e )  {  throw  new CommunicationException  (  "Communication exception: " + e , e ) ; } }   private SSLSocket tunnelThroughProxy  (   final SSLSocketFactory socketFactory )  throws IOException  {   final SSLSocket  socket ;   final String  tunnelHost =  ProxyManager . getProxyHost  ( server ) ;   final Integer  tunnelPort =  ProxyManager . getProxyPort  ( server ) ;   final Socket  tunnel =  new Socket  ( tunnelHost , tunnelPort ) ;   doTunnelHandshake  ( tunnel ,  getServerHost  ( ) ,  getServerPort  ( ) ) ;   socket =  ( SSLSocket )  socketFactory . createSocket  ( tunnel ,  getServerHost  ( ) ,  getServerPort  ( ) , true ) ;   socket . addHandshakeCompletedListener  (  event ->  {   logger . debug  ( "Handshake finished!" ) ;   logger . debug  (  "\t CipherSuite:" +  event . getCipherSuite  ( ) ) ;   logger . debug  (  "\t SessionId " +  event . getSession  ( ) ) ;   logger . debug  (  "\t PeerHost " +   event . getSession  ( ) . getPeerHost  ( ) ) ; } ) ;  return socket ; }   private void doTunnelHandshake  (   final Socket tunnel ,   final String host ,   final  int port )  throws IOException  {   final OutputStream  out =  tunnel . getOutputStream  ( ) ;   final String  msg =       "CONNECT " + host + ":" + port + " HTTP/1.0\n" + "User-Agent: BoardPad Server" + "\r\n\r\n" ;    byte  [ ]  b  [ ] ;  try  {   b =  msg . getBytes  ( "ASCII7" ) ; }  catch (   final  UnsupportedEncodingException ignored )  {   b =  msg . getBytes  ( ) ; }   out . write  ( b ) ;   out . flush  ( ) ;   final   byte  [ ]  reply =  new  byte  [ 200 ] ;   int  replyLen = 0 ;   int  newlinesSeen = 0 ;  boolean  headerDone = false ;   final InputStream  in =  tunnel . getInputStream  ( ) ;  while  (  newlinesSeen < 2 )  {   final  int  i =  in . read  ( ) ;  if  (  i < 0 )  {  throw  new IOException  ( "Unexpected EOF from proxy" ) ; }  if  (  i == '\n' )  {   headerDone = true ;   ++ newlinesSeen ; } else  if  (  i != '\r' )  {   newlinesSeen = 0 ;  if  (   ! headerDone &&  replyLen <  reply . length )  {    reply [  replyLen ++ ] =  (  byte ) i ; } } }  String  replyStr ;  try  {   replyStr =  new String  ( reply , 0 , replyLen , "ASCII7" ) ; }  catch (   final  UnsupportedEncodingException ignored )  {   replyStr =  new String  ( reply , 0 , replyLen ) ; }  if  (  !   replyStr . toLowerCase  ( ) . contains  ( "200 connection established" ) )  {  throw  new IOException  (   "Unable to tunnel through. Proxy returns \"" + replyStr + "\"" ) ; } } }