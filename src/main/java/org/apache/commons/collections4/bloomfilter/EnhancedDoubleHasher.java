  package     org . apache . commons . collections4 . bloomfilter ;   import   java . util . Objects ;  import    java . util . function . IntPredicate ;   public class EnhancedDoubleHasher  implements  Hasher  {   private final  long  initial ;   private final  long  increment ;   private static  long toLong  (   final   byte  [ ] byteArray ,   final  int offset ,   final  int len )  {   long  val = 0 ;   int  shift =  Long . SIZE ;   final  int  end =  offset +  Math . min  ( len ,  Long . BYTES ) ;  for (   int  i = offset ;  i < end ;  i ++ )  {   shift -=  Byte . SIZE ;   val |=   (  long )  (   byteArray [ i ] & 0xFF ) << shift ; }  return val ; }   public EnhancedDoubleHasher  (   final   byte  [ ] buffer )  {  if  (   buffer . length == 0 )  {  throw  new IllegalArgumentException  ( "buffer length must be greater than 0" ) ; }   final  int  segment =   buffer . length / 2 ;    this . initial =  toLong  ( buffer , 0 , segment ) ;    this . increment =  toLong  ( buffer , segment ,   buffer . length - segment ) ; }   public EnhancedDoubleHasher  (   final  long initial ,   final  long increment )  {    this . initial = initial ;    this . increment = increment ; }   long getInitial  ( )  {  return initial ; }   long getIncrement  ( )  {  return increment ; }    @ Override public IndexProducer indices  (   final Shape shape )  {   Objects . requireNonNull  ( shape , "shape" ) ;  return  new IndexProducer  ( )  {    @ Override public boolean forEachIndex  (   final IntPredicate consumer )  {   Objects . requireNonNull  ( consumer , "consumer" ) ;   final  int  bits =  shape . getNumberOfBits  ( ) ;   int  index =  BitMap . mod  ( initial , bits ) ;   int  inc =  BitMap . mod  ( increment , bits ) ;   final  int  k =  shape . getNumberOfHashFunctions  ( ) ;  if  (  k > bits )  {  for (   int  j = k ;  j > 0 ; )  {   final  int  block =  Math . min  ( j , bits ) ;   j -= block ;  for (   int  i = 0 ;  i < block ;  i ++ )  {  if  (  !  consumer . test  ( index ) )  {  return false ; }   index -= inc ;   index =   index < 0 ?  index + bits : index ;   inc -= i ;   inc =   inc < 0 ?  inc + bits : inc ; } } } else  {  for (   int  i = 0 ;  i < k ;  i ++ )  {  if  (  !  consumer . test  ( index ) )  {  return false ; }   index -= inc ;   index =   index < 0 ?  index + bits : index ;   inc -= i ;   inc =   inc < 0 ?  inc + bits : inc ; } }  return true ; }    @ Override public   int  [ ] asIndexArray  ( )  {   final   int  [ ]  result =  new  int  [  shape . getNumberOfHashFunctions  ( ) ] ;   final   int  [ ]  idx =  new  int  [ 1 ] ;   forEachIndex  (  i ->  {    result [   idx [ 0 ] ++ ] = i ;  return true ; } ) ;  return result ; } } ; } }