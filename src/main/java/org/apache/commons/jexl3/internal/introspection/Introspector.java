  package      org . apache . commons . jexl3 . internal . introspection ;   import     org . apache . commons . logging . Log ;  import    java . lang . reflect . Constructor ;  import    java . lang . reflect . Field ;  import    java . lang . reflect . Method ;  import   java . util . ArrayList ;  import   java . util . HashMap ;  import   java . util . Iterator ;  import   java . util . List ;  import   java . util . Map ;  import     java . util . concurrent . locks . ReadWriteLock ;  import     java . util . concurrent . locks . ReentrantReadWriteLock ;   public final class Introspector  {   private static class CacheMiss  {    @ SuppressWarnings  ( "unused" ) public CacheMiss  ( )  { } }   private static final  Constructor  <  ? >  CTOR_MISS =    CacheMiss . class . getConstructors  ( ) [ 0 ] ;   protected final Log  logger ;   private ClassLoader  loader ;   private final Permissions  permissions ;   private final ReadWriteLock  lock =  new ReentrantReadWriteLock  ( ) ;   private final  Map  <  Class  <  ? > , ClassMap >  classMethodMaps =  new  HashMap  <  Class  <  ? > , ClassMap >  ( ) ;   private final  Map  < MethodKey ,  Constructor  <  ? > >  constructorsMap =  new  HashMap  < MethodKey ,  Constructor  <  ? > >  ( ) ;   private final  Map  < String ,  Class  <  ? > >  constructibleClasses =  new  HashMap  < String ,  Class  <  ? > >  ( ) ;   public Introspector  (   final Log log ,   final ClassLoader cloader )  {  this  ( log , cloader , null ) ; }   public Introspector  (   final Log log ,   final ClassLoader cloader ,   final Permissions perms )  {    this . logger = log ;    this . loader = cloader ;    this . permissions =   perms != null ? perms :  Permissions . DEFAULT ; }   public  Class  <  ? > getClassByName  (   final String className )  {  try  {  return  Class . forName  ( className , false , loader ) ; }  catch (   final  ClassNotFoundException xignore )  {  return null ; } }   public Method getMethod  (   final  Class  <  ? > c ,   final String name ,   final  Object  [ ] params )  {  return  getMethod  ( c ,  new MethodKey  ( name , params ) ) ; }   public Method getMethod  (   final  Class  <  ? > c ,   final MethodKey key )  {  try  {  return   getMap  ( c ) . getMethod  ( key ) ; }  catch (   final   MethodKey . AmbiguousException xambiguous )  {  if  (    logger != null &&  xambiguous . isSevere  ( ) &&  logger . isInfoEnabled  ( ) )  {   logger . info  (    "ambiguous method invocation: " +  c . getName  ( ) + "." +  key . debugString  ( ) , xambiguous ) ; }  return null ; } }   public Field getField  (   final  Class  <  ? > c ,   final String key )  {  return   getMap  ( c ) . getField  ( key ) ; }   public  String  [ ] getFieldNames  (   final  Class  <  ? > c )  {  if  (  c == null )  {  return  new String  [ 0 ] ; }   final ClassMap  classMap =  getMap  ( c ) ;  return  classMap . getFieldNames  ( ) ; }   public  String  [ ] getMethodNames  (   final  Class  <  ? > c )  {  if  (  c == null )  {  return  new String  [ 0 ] ; }   final ClassMap  classMap =  getMap  ( c ) ;  return  classMap . getMethodNames  ( ) ; }   public  Method  [ ] getMethods  (   final  Class  <  ? > c ,   final String methodName )  {  if  (  c == null )  {  return null ; }   final ClassMap  classMap =  getMap  ( c ) ;  return  classMap . getMethods  ( methodName ) ; }   public  Constructor  <  ? > getConstructor  (   final MethodKey key )  {  return  getConstructor  ( null , key ) ; }   public  Constructor  <  ? > getConstructor  (   final  Class  <  ? > c ,   final MethodKey key )  {   Constructor  <  ? >  ctor ;    lock . readLock  ( ) . lock  ( ) ;  try  {   ctor =  constructorsMap . get  ( key ) ;  if  (  ctor != null )  {  return   CTOR_MISS . equals  ( ctor ) ? null : ctor ; } }  finally  {    lock . readLock  ( ) . unlock  ( ) ; }    lock . writeLock  ( ) . lock  ( ) ;  try  {   ctor =  constructorsMap . get  ( key ) ;  if  (  ctor != null )  {  return   CTOR_MISS . equals  ( ctor ) ? null : ctor ; }   final String  cname =  key . getMethod  ( ) ;   Class  <  ? >  clazz =  constructibleClasses . get  ( cname ) ;  try  {  if  (  clazz == null )  {  if  (   c != null &&   c . getName  ( ) . equals  (  key . getMethod  ( ) ) )  {   clazz = c ; } else  {   clazz =  loader . loadClass  ( cname ) ; }   constructibleClasses . put  ( cname , clazz ) ; }   final  List  <  Constructor  <  ? > >  l =  new  ArrayList  < >  ( ) ;  for (  final  Constructor  <  ? > ictor :  clazz . getConstructors  ( ) )  {  if  (  permissions . allow  ( ictor ) )  {   l . add  ( ictor ) ; } }   ctor =  key . getMostSpecificConstructor  (  l . toArray  (  new  Constructor  <  ? >  [  l . size  ( ) ] ) ) ;  if  (  ctor != null )  {   constructorsMap . put  ( key , ctor ) ; } else  {   constructorsMap . put  ( key , CTOR_MISS ) ; } }  catch (   final  ClassNotFoundException xnotfound )  {  if  (   logger != null &&  logger . isDebugEnabled  ( ) )  {   logger . debug  (    "unable to find class: " + cname + "." +  key . debugString  ( ) , xnotfound ) ; }   ctor = null ; }  catch (   final   MethodKey . AmbiguousException xambiguous )  {  if  (    logger != null &&  xambiguous . isSevere  ( ) &&  logger . isInfoEnabled  ( ) )  {   logger . info  (    "ambiguous constructor invocation: " + cname + "." +  key . debugString  ( ) , xambiguous ) ; }   ctor = null ; }  return ctor ; }  finally  {    lock . writeLock  ( ) . unlock  ( ) ; } }   private ClassMap getMap  (   final  Class  <  ? > c )  {  ClassMap  classMap ;    lock . readLock  ( ) . lock  ( ) ;  try  {   classMap =  classMethodMaps . get  ( c ) ; }  finally  {    lock . readLock  ( ) . unlock  ( ) ; }  if  (  classMap == null )  {    lock . writeLock  ( ) . lock  ( ) ;  try  {   classMap =  classMethodMaps . get  ( c ) ;  if  (  classMap == null )  {   classMap =  new ClassMap  ( c , permissions , logger ) ;   classMethodMaps . put  ( c , classMap ) ; } }  finally  {    lock . writeLock  ( ) . unlock  ( ) ; } }  return classMap ; }   public void setLoader  (  ClassLoader cloader )  {   final ClassLoader  previous = loader ;  if  (  cloader == null )  {   cloader =   getClass  ( ) . getClassLoader  ( ) ; }  if  (  !  cloader . equals  ( loader ) )  {    lock . writeLock  ( ) . lock  ( ) ;  try  {   final  Iterator  <   Map . Entry  < MethodKey ,  Constructor  <  ? > > >  mentries =   constructorsMap . entrySet  ( ) . iterator  ( ) ;  while  (  mentries . hasNext  ( ) )  {   final   Map . Entry  < MethodKey ,  Constructor  <  ? > >  entry =  mentries . next  ( ) ;   final  Class  <  ? >  clazz =   entry . getValue  ( ) . getDeclaringClass  ( ) ;  if  (  isLoadedBy  ( previous , clazz ) )  {   mentries . remove  ( ) ;   constructibleClasses . remove  (   entry . getKey  ( ) . getMethod  ( ) ) ; } }   final  Iterator  <   Map . Entry  <  Class  <  ? > , ClassMap > >  centries =   classMethodMaps . entrySet  ( ) . iterator  ( ) ;  while  (  centries . hasNext  ( ) )  {   final   Map . Entry  <  Class  <  ? > , ClassMap >  entry =  centries . next  ( ) ;   final  Class  <  ? >  clazz =  entry . getKey  ( ) ;  if  (  isLoadedBy  ( previous , clazz ) )  {   centries . remove  ( ) ; } }   loader = cloader ; }  finally  {    lock . writeLock  ( ) . unlock  ( ) ; } } }   public ClassLoader getLoader  ( )  {  return loader ; }   private static boolean isLoadedBy  (   final ClassLoader loader ,   final  Class  <  ? > clazz )  {  if  (  loader != null )  {  ClassLoader  cloader =  clazz . getClassLoader  ( ) ;  while  (  cloader != null )  {  if  (  cloader . equals  ( loader ) )  {  return true ; }   cloader =  cloader . getParent  ( ) ; } }  return false ; } }