  package   org . asteriskjava . fastagi ;   import     org . asteriskjava . fastagi . internal . AgiChannelFactory ;  import     org . asteriskjava . fastagi . internal . DefaultAgiChannelFactory ;  import    org . asteriskjava . util . DaemonThreadFactory ;  import    org . asteriskjava . util . Log ;  import    org . asteriskjava . util . LogFactory ;  import    java . util . concurrent . RejectedExecutionException ;  import    java . util . concurrent . SynchronousQueue ;  import    java . util . concurrent . ThreadPoolExecutor ;  import    java . util . concurrent . TimeUnit ;   public abstract class AbstractAgiServer  {   private final Log  logger =  LogFactory . getLog  (  getClass  ( ) ) ;   private static final  int  DEFAULT_POOL_SIZE = 10 ;   private static final  int  DEFAULT_MAXIMUM_POOL_SIZE = 100 ;   private  int  poolSize = DEFAULT_POOL_SIZE ;   private  int  maximumPoolSize = DEFAULT_MAXIMUM_POOL_SIZE ;   private ThreadPoolExecutor  pool ;   private MappingStrategy  mappingStrategy ;   private final AgiChannelFactory  agiChannelFactory ;   private volatile boolean  die = false ;   public AbstractAgiServer  ( )  {  this  (  new DefaultAgiChannelFactory  ( ) ) ; }   public AbstractAgiServer  (  AgiChannelFactory agiChannelFactory )  {  if  (  agiChannelFactory == null )  {  throw  new IllegalArgumentException  ( "AgiChannelFactory must not be null" ) ; }   logger . debug  (  "Using channelFactory " +   agiChannelFactory . getClass  ( ) . getCanonicalName  ( ) ) ;    this . agiChannelFactory = agiChannelFactory ; }   protected AgiChannelFactory getAgiChannelFactory  ( )  {  return  this . agiChannelFactory ; }   public  int getPoolSize  ( )  {  return poolSize ; }   public synchronized void setPoolSize  (   int poolSize )  {  if  (  poolSize < 0 )  {  throw  new IllegalArgumentException  (   "New poolSize (" + poolSize + ") is must not be negative" ) ; }  if  (  pool != null )  {   pool . setCorePoolSize  ( poolSize ) ; }    this . poolSize = poolSize ; }   public  int getMaximumPoolSize  ( )  {  return maximumPoolSize ; }   public synchronized void setMaximumPoolSize  (   int maximumPoolSize )  {  if  (  maximumPoolSize <= 0 )  {  throw  new IllegalArgumentException  (   "New maximumPoolSize (" + maximumPoolSize + ") is must be positive" ) ; }  if  (  maximumPoolSize < poolSize )  {  throw  new IllegalArgumentException  (     "New maximumPoolSize (" + maximumPoolSize + ") is less than current pool size (" + poolSize + ")" ) ; }  if  (  pool != null )  {   pool . setMaximumPoolSize  ( maximumPoolSize ) ; }    this . maximumPoolSize = maximumPoolSize ; }   public void setMappingStrategy  (  MappingStrategy mappingStrategy )  {    this . mappingStrategy = mappingStrategy ; }   protected MappingStrategy getMappingStrategy  ( )  {  return mappingStrategy ; }   protected boolean isDie  ( )  {  return die ; }   protected synchronized void shutdown  ( )  {    this . die = true ;  if  (  pool != null )  {   pool . shutdown  ( ) ; } }    @ Override protected void finalize  ( )  throws Throwable  {   super . finalize  ( ) ;    this . die = true ;  if  (  pool != null )  {   pool . shutdown  ( ) ; } }   protected void execute  (  Runnable command )  throws RejectedExecutionException  {  if  (  isDie  ( ) )  {   logger . warn  ( "AgiServer is shutting down: Refused to execute AgiScript" ) ;  return ; }    getPool  ( ) . execute  ( command ) ; }   protected void handleException  (  String message ,  Exception e )  {   logger . warn  ( message , e ) ; }   private synchronized ThreadPoolExecutor getPool  ( )  {  if  (  pool == null )  {   pool =  createPool  ( ) ;   logger . info  ( "Thread pool started." ) ; }  return pool ; }   public  int getPoolActiveTaskCount  ( )  {  if  (  pool != null )  {  return  pool . getActiveCount  ( ) ; }  return  - 1 ; }   public  int getPoolActiveThreadCount  ( )  {  if  (  pool != null )  {  return  pool . getPoolSize  ( ) ; }  return  - 1 ; }   protected ThreadPoolExecutor createPool  ( )  {  return  new ThreadPoolExecutor  ( poolSize ,   (  maximumPoolSize < poolSize ) ? poolSize : maximumPoolSize , 50000L ,  TimeUnit . MILLISECONDS ,  new  SynchronousQueue  < Runnable >  ( ) ,  new DaemonThreadFactory  ( ) ) ; } }