  package     net . openhft . performance . tests . network ;   import     net . openhft . chronicle . bytes . Bytes ;  import      net . openhft . chronicle . core . threads . EventLoop ;  import      net . openhft . chronicle . core . threads . HandlerPriority ;  import      net . openhft . chronicle . core . threads . ThreadDump ;  import    net . openhft . chronicle . network .  * ;  import      net . openhft . chronicle . network . connection . TcpChannelHub ;  import      net . openhft . chronicle . network . connection . TryLock ;  import     net . openhft . chronicle . threads . EventGroup ;  import     net . openhft . chronicle . wire . TextWire ;  import     net . openhft . chronicle . wire . Wire ;  import     net . openhft . chronicle . wire . WireType ;  import    org . jetbrains . annotations . NotNull ;  import   org . junit . After ;  import   org . junit . Assert ;  import   org . junit . Before ;  import   org . junit . Test ;  import   java . io . IOException ;  import    java . nio . channels . SocketChannel ;  import    java . util . concurrent . TimeoutException ;  import    java . util . function . Function ;  import static       net . openhft . chronicle . network . connection . SocketAddressSupplier . uri ;  import static       net . openhft . performance . tests . network . LegacyHanderFactory . simpleTcpEventHandlerFactory ;   public class SimpleServerAndClientTest  {   private ThreadDump  threadDump ;    @ Before public void threadDump  ( )  {   threadDump =  new ThreadDump  ( ) ; }    @ After public void checkThreadDump  ( )  {   threadDump . assertNoNewThreads  ( ) ; }    @ Test public void test  ( )  throws IOException , TimeoutException , InterruptedException  {  for ( ; ; )  {   final String  desc = "host.port" ;   TCPRegistry . createServerSocketChannelFor  ( desc ) ;  try  (  EventLoop eg =  new EventGroup  ( true ) )  {   eg . start  ( ) ;   final String  expectedMessage = "<my message>" ;   createServer  ( desc , eg ) ;  try  (  TcpChannelHub tcpChannelHub =  createClient  ( eg , desc ) )  {   final  long  tid =  tcpChannelHub . nextUniqueTransaction  (  System . currentTimeMillis  ( ) ) ;   final Wire  wire =  new TextWire  (  Bytes . elasticByteBuffer  ( ) ) ;   wire . writeDocument  ( true ,  w ->   w . write  (   ( ) -> "tid" ) . int64  ( tid ) ) ;   wire . writeDocument  ( false ,  w ->   w . write  (   ( ) -> "payload" ) . text  ( expectedMessage ) ) ;   tcpChannelHub . lock2  (   ( ) ->  tcpChannelHub . writeSocket  ( wire , true ) , true ,  TryLock . TRY_LOCK_WARN ) ;  try  {  Wire  reply =  tcpChannelHub . proxyReply  ( 500 , tid ) ;   reply . readDocument  ( null ,  data ->  {   final String  text =   data . read  (   ( ) -> "payloadResponse" ) . text  ( ) ;   Assert . assertEquals  ( expectedMessage , text ) ; } ) ; }  catch (   TimeoutException e )  {  continue ; }  break ; } }  finally  {   TcpChannelHub . closeAllHubs  ( ) ;   TCPRegistry . reset  ( ) ; } } 
<<<<<<<
 try  (  EventLoop eg =  new EventGroup  ( true ) )  {   eg . start  ( ) ;   final String  expectedMessage = "<my message>" ;   createServer  ( desc , eg ) ;   Thread . sleep  ( 1 ) ;  try  (  TcpChannelHub tcpChannelHub =  createClient  ( eg , desc ) )  {   final  long  tid =  tcpChannelHub . nextUniqueTransaction  (  System . currentTimeMillis  ( ) ) ;   final Wire  wire =  new TextWire  (  Bytes . elasticByteBuffer  ( ) ) ;   wire . writeDocument  ( true ,  w ->   w . write  (   ( ) -> "tid" ) . int64  ( tid ) ) ;   wire . writeDocument  ( false ,  w ->   w . write  (   ( ) -> "payload" ) . text  ( expectedMessage ) ) ;   tcpChannelHub . lock2  (   ( ) ->  tcpChannelHub . writeSocket  ( wire , true ) , true ,  TryLock . TRY_LOCK_WARN ) ;  Wire  reply =  tcpChannelHub . proxyReply  (   TimeUnit . SECONDS . toMillis  ( 5 ) , tid ) ;   reply . readDocument  ( null ,  data ->  {   final String  text =   data . read  (   ( ) -> "payloadResponse" ) . text  ( ) ;   Assert . assertEquals  ( expectedMessage , text ) ; } ) ; } }  finally  {   TcpChannelHub . closeAllHubs  ( ) ;   TCPRegistry . reset  ( ) ; }
=======
>>>>>>>
 }    @ NotNull private TcpChannelHub createClient  (  EventLoop eg ,  String desc )  {  return  new TcpChannelHub  ( null , eg ,  WireType . TEXT , "/" ,  uri  ( desc ) , false , null ,  HandlerPriority . TIMER ) ; }   private void createServer  (  String desc ,  EventLoop eg )  throws IOException  {  AcceptorEventHandler  eah =  new AcceptorEventHandler  ( desc ,  handlerFactory  ( ) ,  VanillaNetworkContext :: new ) ;   eg . addHandler  ( eah ) ;  SocketChannel  sc =  TCPRegistry . createSocketChannel  ( desc ) ;   sc . configureBlocking  ( false ) ; }    @ NotNull private  Function  < NetworkContext , TcpEventHandler > handlerFactory  ( )  {  return  simpleTcpEventHandlerFactory  (  WireEchoRequestHandler :: new ,  WireType . TEXT ) ; } }