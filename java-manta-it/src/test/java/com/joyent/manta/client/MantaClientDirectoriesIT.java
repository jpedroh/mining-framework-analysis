  package    com . joyent . manta . client ;   import     com . joyent . manta . config . IntegrationTestConfigContext ;  import     com . joyent . test . util . MantaAssert ;  import     com . joyent . test . util . MantaFunction ;  import     org . apache . commons . lang3 . RandomUtils ;  import     org . apache . commons . text . RandomStringGenerator ;  import   org . slf4j . Logger ;  import   org . slf4j . LoggerFactory ;  import   org . testng . Assert ;  import    org . testng . annotations . AfterClass ;  import    org . testng . annotations . BeforeClass ;  import    org . testng . annotations . Test ;  import   java . io . IOException ;  import   java . util . UUID ;  import static      com . joyent . manta . client . MantaClient . SEPARATOR ;  import static      com . joyent . manta . exception . MantaErrorCode . RESOURCE_NOT_FOUND_ERROR ;  import static      com . joyent . manta . util . MantaUtils . writeablePrefixPaths ;    @ Test  (  groups =  { "directory" } ) public class MantaClientDirectoriesIT  {   private static final Logger  LOG =  LoggerFactory . getLogger  (  MantaClientDirectoriesIT . class ) ;   private static final RandomStringGenerator  STRING_GENERATOR =    new  RandomStringGenerator . Builder  ( ) . withinRange  (  (  int ) 'a' ,  (  int ) 'z' ) . build  ( ) ;   private static final String  TEST_DATA = "EPISODEII_IS_BEST_EPISODE" ;   private MantaClient  mantaClient ;   private String  testPathPrefix ;   private IntegrationTestConfigContext  config ;    @ BeforeClass public void beforeClass  ( )  throws IOException  {   config =  new IntegrationTestConfigContext  ( ) ;   mantaClient =  new MantaClient  ( config ) ;   testPathPrefix =  IntegrationTestConfigContext . generateBasePath  ( config ,   this . getClass  ( ) . getSimpleName  ( ) ) ; }    @ AfterClass public void afterClass  ( )  throws IOException  {   IntegrationTestConfigContext . cleanupTestDirectory  ( mantaClient , testPathPrefix ) ; }    @ Test public void canCreateDirectory  ( )  throws IOException  {   mantaClient . putDirectory  ( testPathPrefix , true ) ;  String  dir =  String . format  ( "%s/%s" , testPathPrefix ,  UUID . randomUUID  ( ) ) ;  boolean  created =  mantaClient . putDirectory  ( dir ) ;   Assert . assertTrue  ( created , "Directory was marked as created" ) ;  MantaObject  response =  mantaClient . head  ( dir ) ;   Assert . assertEquals  ( dir ,  response . getPath  ( ) ) ; }    @ Test public void willReturnFalseWhenWeOverwriteDirectory  ( )  throws IOException  {   mantaClient . putDirectory  ( testPathPrefix ) ;  String  dir =  String . format  ( "%s/%s" , testPathPrefix ,  UUID . randomUUID  ( ) ) ;   Assert . assertTrue  (  mantaClient . putDirectory  ( dir ) , "We were unable to create the initial directory" ) ;  boolean  result =  mantaClient . putDirectory  ( dir ) ;   Assert . assertFalse  ( result ,  "Expected a false value because we " + "didn't create a new directory" ) ; }    @ Test  (  dependsOnMethods =  { "canCreateDirectory" } ) public void canDeleteDirectory  ( )  throws IOException  {  String  dir =  String . format  ( "%s/%s" , testPathPrefix ,  UUID . randomUUID  ( ) ) ;   mantaClient . putDirectory  ( dir ) ;  MantaObject  response =  mantaClient . head  ( dir ) ;   Assert . assertEquals  ( dir ,  response . getPath  ( ) ) ;   mantaClient . delete  ( dir ) ;   MantaAssert . assertResponseFailureStatusCode  ( 404 , RESOURCE_NOT_FOUND_ERROR ,  (  MantaFunction  < Object > )   ( ) ->  mantaClient . get  ( dir ) ) ; }    @ Test  (  dependsOnMethods =  { "canCreateDirectory" } ) public void wontErrorWhenWeCreateOverAnExistingDirectory  ( )  throws IOException  {  String  dir =  String . format  ( "%s/%s" , testPathPrefix ,  UUID . randomUUID  ( ) ) ;   mantaClient . putDirectory  ( dir ) ;   mantaClient . putDirectory  ( dir ) ;   mantaClient . putDirectory  ( dir ) ;  MantaObject  response =  mantaClient . head  ( dir ) ;   Assert . assertEquals  ( dir ,  response . getPath  ( ) ) ; }    @ Test  (  dependsOnMethods =  { "canCreateDirectory" } ) public void noErrorWhenWeOverwriteAnExistingFile  ( )  throws IOException  {  String  dir =  String . format  ( "%s/%s" , testPathPrefix ,  UUID . randomUUID  ( ) ) ;   mantaClient . putDirectory  ( dir ) ;  String  file =  String . format  ( "%s/%s" , dir ,  UUID . randomUUID  ( ) ) ;   mantaClient . put  ( file , TEST_DATA ) ;   mantaClient . putDirectory  ( file ) ; }    @ Test  (  dependsOnMethods =  { "canCreateDirectory" } ) public void directoryIsMarkedAsSuch  ( )  throws IOException  {  MantaObject  dir =  mantaClient . head  ( testPathPrefix ) ;   Assert . assertTrue  (  dir . isDirectory  ( ) ,  String . format  (  "Directory should be marked as such [%s]. " + "\nResponse: %s" , testPathPrefix , dir ) ) ; }    @ Test  (  dependsOnMethods =  { "wontErrorWhenWeCreateOverAnExistingDirectory" } ) public void canRecursivelyCreateDirectory  ( )  throws IOException  {  String  dir =  String . format  ( "%s/%s/%s/%s/%s/%s" , testPathPrefix ,  UUID . randomUUID  ( ) ,  UUID . randomUUID  ( ) ,  UUID . randomUUID  ( ) ,  UUID . randomUUID  ( ) ,  UUID . randomUUID  ( ) ) ;   mantaClient . putDirectory  ( dir , true ) ;  MantaObject  response =  mantaClient . head  ( dir ) ;   Assert . assertTrue  (  response . isDirectory  ( ) ,  String . format  ( "Directory should be marked as such [%s]" , testPathPrefix ) ) ;   Assert . assertEquals  ( dir ,  response . getPath  ( ) ) ; }    @ Test public void canCreateDirectoriesNormallyWhenNewDirectoryDepthLessThanSkipDepth  ( )  throws IOException  {  if  (  !  mantaClient . existsAndIsAccessible  ( testPathPrefix ) )  {   Assert . fail  ( "Base directory is missing" ) ; }   final String  childDirectory =   testPathPrefix + SEPARATOR +  UUID . randomUUID  ( ) ;   final  int  childDirectoryDepth =   writeablePrefixPaths  ( childDirectory ) . length ;   config . setSkipDirectoryDepth  (  childDirectoryDepth + 1 ) ;   final  long  operations =  RecursiveDirectoryCreationStrategy . createWithSkipDepth  ( mantaClient , childDirectory , null ,  config . getSkipDirectoryDepth  ( ) ) ;   Assert . assertEquals  ( operations , childDirectoryDepth ) ; }    @ Test public void canSkipAlreadyCreatedDirectoriesWhenDepthSetCorrectly  ( )  throws IOException  {   directorySkipping  ( true ) ; }    @ Test public void canAddAtMostOneFailedOperationWhenDepthSetIncorrectly  ( )  throws IOException  {   directorySkipping  ( false ) ; }   private void directorySkipping  (   final boolean settingCorrectDepth )  throws IOException  {   final  int  parentAddedDepth =  RandomUtils . nextInt  ( 1 , 5 ) ;   final  int  childAddedDepth =  RandomUtils . nextInt  ( 2 , 5 ) ;   final StringBuilder  parentDirBuilder =  new StringBuilder  ( testPathPrefix ) ;  for (   int  i = 0 ;  i < parentAddedDepth ;  i ++ )  {    parentDirBuilder . append  ( SEPARATOR ) . append  (  STRING_GENERATOR . generate  ( 3 ) ) ; }   final String  parentDir =  parentDirBuilder . toString  ( ) ;   final  int  parentDirDepth =   writeablePrefixPaths  ( parentDir ) . length ;   config . setSkipDirectoryDepth  ( parentDirDepth ) ;  if  (  ! settingCorrectDepth )  {   config . setSkipDirectoryDepth  (   config . getSkipDirectoryDepth  ( ) + 1 ) ; }   final StringBuilder  childDirBuilder =  new StringBuilder  ( parentDir ) ;  for (   int  i = 0 ;  i < childAddedDepth ;  i ++ )  {    childDirBuilder . append  ( SEPARATOR ) . append  (  STRING_GENERATOR . generate  ( 3 ) ) ; }   final String  childDir =  childDirBuilder . toString  ( ) ;   mantaClient . putDirectory  ( parentDir , true ) ;   final MantaObject  response =  mantaClient . head  ( parentDir ) ;   Assert . assertTrue  (  response . isDirectory  ( ) ,  String . format  ( "Directory should be marked as such [%s]" , testPathPrefix ) ) ;   Assert . assertEquals  ( parentDir ,  response . getPath  ( ) ) ;   final  long  operations =  RecursiveDirectoryCreationStrategy . createWithSkipDepth  ( mantaClient , childDir , null ,  config . getSkipDirectoryDepth  ( ) ) ;   final MantaObject  nestedResponse =  mantaClient . head  ( childDir ) ;   Assert . assertTrue  (  nestedResponse . isDirectory  ( ) ,  String . format  ( "Nested directory should be marked as such [%s]" , testPathPrefix ) ) ;   Assert . assertEquals  ( childDir ,  nestedResponse . getPath  ( ) ) ;   LOG . info  (  "PARENT " + parentDir ) ;   LOG . info  (  "CHILD  " + childDir ) ;   LOG . info  (  "calls (child): " + operations ) ;  if  ( settingCorrectDepth )  {   Assert . assertEquals  ( operations , childAddedDepth ) ; } else  {   Assert . assertEquals  ( operations ,   parentDirDepth + childAddedDepth + 1 ) ; } }    @ Test public void pruneParentDirectoriesFull  ( )  throws IOException  {   final String  parentDir =  createRandomDirectory  ( testPathPrefix , 1 ) ;   createRandomDirectory  ( testPathPrefix , 1 ) ;   final String  childDir =  createRandomDirectory  ( parentDir , 5 ) ;   LOG . info  (  "CHILD DIR  : " + childDir ) ;   LOG . info  (  "Parent DIR : " + parentDir ) ;   mantaClient . delete  ( childDir , null ,  PruneEmptyParentDirectoryStrategy . PRUNE_ALL_PARENTS ) ;   Assert . assertFalse  (  mantaClient . existsAndIsAccessible  ( childDir ) ) ;   Assert . assertFalse  (  mantaClient . existsAndIsAccessible  ( parentDir ) ) ;  String  ancestor =  parentDir . substring  ( 0 ,  parentDir . lastIndexOf  ( SEPARATOR ) ) ;   Assert . assertTrue  (  mantaClient . existsAndIsAccessible  ( ancestor ) ) ; }    @ Test public void pruneParentDirectoryZero  ( )  throws IOException  {   final String  parentDir =  createRandomDirectory  ( testPathPrefix , 1 ) ;   final String  childDir =  createRandomDirectory  ( parentDir , 2 ) ;   LOG . debug  (  "CHILD DIR  : " + childDir ) ;   LOG . debug  (  "Parent DIR : " + parentDir ) ;   mantaClient . delete  ( childDir , null , 0 ) ;   Assert . assertFalse  (  mantaClient . existsAndIsAccessible  ( childDir ) ) ;   Assert . assertTrue  (  mantaClient . existsAndIsAccessible  ( parentDir ) ) ; }    @ Test public void pruneParentDirectoryOne  ( )  throws IOException  {   final String  parentDir =  createRandomDirectory  ( testPathPrefix , 1 ) ;   final String  childDir =  createRandomDirectory  ( parentDir , 3 ) ;   LOG . info  (  "CHILD DIR  : " + childDir ) ;   LOG . info  (  "Parent DIR : " + parentDir ) ;   mantaClient . delete  ( childDir , null , 1 ) ;   Assert . assertFalse  (  mantaClient . existsAndIsAccessible  ( childDir ) ) ;   Assert . assertTrue  (  mantaClient . existsAndIsAccessible  ( parentDir ) ) ; }   public  @ Test  (  expectedExceptions =  IllegalArgumentException . class ) void pruneParentDirectoriesInvalid  ( )  throws IOException  {   final String  parentDir =  createRandomDirectory  ( testPathPrefix , 1 ) ;   final String  childDir =  createRandomDirectory  ( parentDir , 5 ) ;   Assert . assertThrows  (    java . lang . IllegalArgumentException . class ,   ( ) ->  {   mantaClient . delete  ( childDir , null ,  - 3 ) ; } ) ; }    @ Test public void pruneParentDirectoriesWithFile  ( )  throws IOException  {   final String  parentDir =  createRandomDirectory  ( testPathPrefix , 1 ) ;   final String  childDir =  createRandomDirectory  ( parentDir , 5 ) ;   LOG . debug  (  "CHILD DIR  : " + childDir ) ;   LOG . debug  (  "Parent DIR : " + parentDir ) ;  String  file =  String . format  ( "%s/%s" , childDir ,  UUID . randomUUID  ( ) ) ;   mantaClient . put  ( file , TEST_DATA ) ;   mantaClient . putDirectory  ( file ) ;   LOG . debug  (  "CHILD DIR  : " + childDir ) ;   mantaClient . delete  ( file , null , 1 ) ;   Assert . assertFalse  (  mantaClient . existsAndIsAccessible  ( file ) ) ;   Assert . assertFalse  (  mantaClient . existsAndIsAccessible  ( childDir ) ) ;   Assert . assertTrue  (  mantaClient . existsAndIsAccessible  ( parentDir ) ) ; }    @ Test public void pruneParentDirectoriesFailingWithFile  ( )  throws IOException  {   final String  parentDir =  createRandomDirectory  ( testPathPrefix , 1 ) ;   final String  childDir =  createRandomDirectory  ( parentDir , 5 ) ;   LOG . debug  (  "CHILD DIR  : " + childDir ) ;   LOG . debug  (  "Parent DIR : " + parentDir ) ;  String  file =  String . format  ( "%s/%s" , childDir ,  UUID . randomUUID  ( ) ) ;   mantaClient . put  ( file , TEST_DATA ) ;   mantaClient . putDirectory  ( file ) ;  String  file2 =  String . format  ( "%s/%s" , childDir ,  UUID . randomUUID  ( ) ) ;   mantaClient . put  ( file2 , TEST_DATA ) ;   mantaClient . putDirectory  ( file2 ) ;   mantaClient . delete  ( file , null , 2 ) ;   Assert . assertFalse  (  mantaClient . existsAndIsAccessible  ( file ) ) ;   Assert . assertTrue  (  mantaClient . existsAndIsAccessible  ( file2 ) ) ;   Assert . assertTrue  (  mantaClient . existsAndIsAccessible  ( parentDir ) ) ; }    @ Test public void pruneParentDirectoriesFailingGreaterThanDirPath  ( )  throws IOException  {   final String  parentDir =  createRandomDirectory  ( testPathPrefix , 1 ) ;   final String  childDir =  createRandomDirectory  ( parentDir , 2 ) ;   mantaClient . delete  ( childDir , null , 10 ) ;   Assert . assertFalse  (  mantaClient . existsAndIsAccessible  ( parentDir ) ) ;   Assert . assertFalse  (  mantaClient . existsAndIsAccessible  ( childDir ) ) ; }   private String createRandomDirectory  (   final String parent ,   final  int depth )  throws IOException  {   final StringBuilder  parentDirBuilder =  new StringBuilder  ( parent ) ;  for (   int  i = 0 ;  i < depth ;  i ++ )  {    parentDirBuilder . append  ( SEPARATOR ) . append  (  STRING_GENERATOR . generate  ( 3 ) ) ; }   final String  dirPath =  parentDirBuilder . toString  ( ) ;   mantaClient . putDirectory  ( dirPath , true ) ;  return dirPath ; } }