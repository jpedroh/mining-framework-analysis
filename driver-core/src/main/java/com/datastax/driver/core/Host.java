  package    com . datastax . driver . core ;   import   java . net . InetAddress ;  import   java . net . InetSocketAddress ;  import     java . util . concurrent . atomic . AtomicReference ;  import     com . google . common . collect . ImmutableList ;  import      com . google . common . util . concurrent . Futures ;  import      com . google . common . util . concurrent . ListenableFuture ;  import   org . slf4j . Logger ;  import   org . slf4j . LoggerFactory ;  import      com . datastax . driver . core . policies . ReconnectionPolicy ;   public class Host  {   private static final Logger  logger =  LoggerFactory . getLogger  (  Host . class ) ;   private final InetSocketAddress  address ;  enum State  {  ADDED ,  DOWN ,  SUSPECT ,  UP }   volatile State  state ;   private final ConvictionPolicy  policy ;   final  AtomicReference  <  ListenableFuture  <  ? > >  initialReconnectionAttempt =  new  AtomicReference  <  ListenableFuture  <  ? > >  (  Futures . immediateFuture  ( null ) ) ;   final  AtomicReference  <  ListenableFuture  <  ? > >  reconnectionAttempt =  new  AtomicReference  <  ListenableFuture  <  ? > >  ( ) ;   final ExecutionInfo  defaultExecutionInfo ;   private volatile String  datacenter ;   private volatile String  rack ;   private volatile VersionNumber  cassandraVersion ;   volatile InetAddress  listenAddress ;  void setLocationInfo  (  String datacenter ,  String rack )  {    this . datacenter = datacenter ;    this . rack = rack ; }  void setVersionAndListenAdress  (  String cassandraVersion ,  InetAddress listenAddress )  {  if  (  listenAddress != null )    this . listenAddress = listenAddress ;  if  (  cassandraVersion == null )  return ;  try  {    this . cassandraVersion =  VersionNumber . parse  ( cassandraVersion ) ; }  catch (   IllegalArgumentException e )  {   logger . warn  ( "Error parsing Cassandra version {}. This shouldn't have happened" , cassandraVersion ) ; } }   public InetAddress getAddress  ( )  {  return  address . getAddress  ( ) ; }   public InetSocketAddress getSocketAddress  ( )  {  return address ; }   public String getDatacenter  ( )  {  return datacenter ; }   public String getRack  ( )  {  return rack ; }   public VersionNumber getCassandraVersion  ( )  {  return cassandraVersion ; }   public boolean isUp  ( )  {  return   state ==  State . UP ||  state ==  State . SUSPECT ; }   public  ListenableFuture  <  ? > getInitialReconnectionAttemptFuture  ( )  {  return  initialReconnectionAttempt . get  ( ) ; }    @ Override public  int hashCode  ( )  {  return  address . hashCode  ( ) ; }  boolean wasJustAdded  ( )  {  return  state ==  State . ADDED ; }    @ Override public String toString  ( )  {  return  address . toString  ( ) ; }  void setDown  ( )  {   state =  State . DOWN ; }  void setUp  ( )  {   policy . reset  ( ) ;   state =  State . UP ; }  boolean setSuspected  ( )  {  if  (  state !=  State . UP )  return false ;   state =  State . SUSPECT ;  return true ; }  boolean signalConnectionFailure  (  ConnectionException exception )  {  return  policy . addFailure  ( exception ) ; }   public interface StateListener  {   public void onAdd  (  Host host ) ;   public void onUp  (  Host host ) ;   public void onSuspected  (  Host host ) ;   public void onDown  (  Host host ) ;   public void onRemove  (  Host host ) ; }   private final  Cluster . Manager  manager ;  Host  (  InetSocketAddress address ,   ConvictionPolicy . Factory policy ,   Cluster . Manager manager )  {  if  (   address == null ||  policy == null )  throw  new NullPointerException  ( ) ;    this . address = address ;    this . policy =  policy . create  ( this ) ;    this . manager = manager ;    this . defaultExecutionInfo =  new ExecutionInfo  (  ImmutableList . of  ( this ) ) ;    this . state =  State . ADDED ; }   public  ListenableFuture  <  ? > getReconnectionAttemptFuture  ( )  {  return  reconnectionAttempt . get  ( ) ; }   public void tryReconnectOnce  ( )  {    this . manager . tryReconnectOnce  ( this ) ; }    @ Override public boolean equals  (  Object other )  {  if  (  other instanceof Host )  {  Host  that =  ( Host ) other ;  return   this . address . equals  (  that . address ) ; }  return false ; } }