  package    com . datastax . driver . core ;   import   java . util . Collection ;  import   java . util . Collections ;  import    org . testng . annotations . Test ;  import static   org . testng . Assert .  * ;  import      com . datastax . driver . core . exceptions . UnsupportedFeatureException ;   public class FetchingTest  extends  CCMBridge . PerClassSingleNodeCluster  {    @ Override protected  Collection  < String > getTableDefinitions  ( )  {  return  Collections . singletonList  ( "CREATE TABLE test (k text, v int, PRIMARY KEY (k, v))" ) ; }    @ Test  (  groups = "short" ) public void simplePagingTest  ( )  throws Throwable  {  try  {  String  key = "paging_test" ;  for (   int  i = 0 ;  i < 100 ;  i ++ )   session . execute  (  String . format  ( "INSERT INTO test (k, v) VALUES ('%s', %d)" , key , i ) ) ;  SimpleStatement  st =  new SimpleStatement  (  String . format  ( "SELECT v FROM test WHERE k='%s'" , key ) ) ;   st . setFetchSize  ( 5 ) ;  ResultSet  rs =  session . execute  ( st ) ;   assertFalse  (  rs . isFullyFetched  ( ) ) ;  for (   int  i = 0 ;  i < 100 ;  i ++ )  {   assertFalse  (  rs . isExhausted  ( ) ) ;   assertEquals  (  rs . getAvailableWithoutFetching  ( ) ,  5 -  (  i % 5 ) ) ;   assertEquals  (   rs . one  ( ) . getInt  ( 0 ) , i ) ; }   assertTrue  (  rs . isExhausted  ( ) ) ;   assertTrue  (  rs . isFullyFetched  ( ) ) ; }  catch (   UnsupportedFeatureException e )  {  if  (     cluster . getConfiguration  ( ) . getProtocolOptions  ( ) . getProtocolVersion  ( ) !=  ProtocolVersion . V1 )  throw e ; }  catch (   Throwable e )  {   errorOut  ( ) ;  throw e ; } } }