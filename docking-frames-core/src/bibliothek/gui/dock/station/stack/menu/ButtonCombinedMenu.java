  package      bibliothek . gui . dock . station . stack . menu ;   import   java . awt . Component ;  import   javax . swing . Icon ;  import   bibliothek . gui . DockController ;  import   bibliothek . gui . Dockable ;  import     bibliothek . gui . dock . action . ActionContentModifier ;  import      bibliothek . gui . dock . station . stack . CombinedHandler ;  import      bibliothek . gui . dock . station . stack . CombinedMenu ;  import       bibliothek . gui . dock . station . stack . tab . TabMenuDockIcon ;  import       bibliothek . gui . dock . station . stack . tab . TabPane ;  import       bibliothek . gui . dock . themes . basic . action . BasicButtonModel ;  import       bibliothek . gui . dock . themes . basic . action . BasicTrigger ;  import      bibliothek . gui . dock . themes . icon . TabMenuOverflowIconBridge ;  import     bibliothek . gui . dock . util . BackgroundAlgorithm ;  import     bibliothek . gui . dock . util . BackgroundPaint ;  import     bibliothek . gui . dock . action . DockAction ;   public abstract class ButtonCombinedMenu  <  B  extends Component >  extends AbstractCombinedMenu  {   private B  button ;   private TabMenuDockIcon  icon ;   private Icon  currentIcon ;   public ButtonCombinedMenu  (  TabPane pane ,   CombinedHandler  <  ? super AbstractCombinedMenu > handler )  {  super  ( pane , handler ) ;   icon =  new TabMenuDockIcon  (  TabMenuOverflowIconBridge . ICON_KEY , this )  {   protected void changed  (  Icon oldValue ,  Icon newValue )  {   currentIcon = newValue ;  if  (  button != null )  {    getModel  ( button ) . setIcon  (  ActionContentModifier . NONE_HORIZONTAL , newValue ) ; } } } ; }    @ Override public void setController  (  DockController controller )  {   super . setController  ( controller ) ;  if  (  controller == null )  {   icon . setManager  ( null ) ; } else  {   icon . setManager  (  controller . getIcons  ( ) ) ; } }    @ Override protected void backgroundChanged  (  BackgroundPaint paint )  {  if  (  button != null )  {    getModel  ( button ) . setBackground  ( paint ,  getBackground  ( ) ) ; } }    @ Override protected Component createComponent  ( )  {  BasicTrigger  trigger =  new BasicTrigger  ( )  {   public void triggered  ( )  {   open  ( ) ; }   public DockAction getAction  ( )  {  return null ; }   public Dockable getDockable  ( )  {  return null ; } } ;   button =  createButton  ( trigger ) ;    getModel  ( button ) . setIcon  (  ActionContentModifier . NONE_HORIZONTAL , currentIcon ) ;  return button ; }   public B getButton  ( )  {  return button ; }   protected abstract B createButton  (  BasicTrigger trigger ) ;   protected abstract BasicButtonModel getModel  (  B button ) ;    @ Override protected void ensureComponent  ( )  {  boolean  set =  button == null ;   super . ensureComponent  ( ) ;  if  ( set )  {  BackgroundAlgorithm  background =  getBackground  ( ) ;    getModel  ( button ) . setBackground  (  background . getPaint  ( ) , background ) ; } }    @ Override protected void selected  (  Dockable dockable )  {    getTabParent  ( ) . setSelectedDockable  ( dockable ) ; } }